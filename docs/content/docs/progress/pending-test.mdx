---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- v0.6.4 Windows 启动修复：在未装开发依赖的干净 Windows 用户目录中覆盖 v0.6.3 后启动 v0.6.4；应用不得出现 `Cannot find module './canvas-recovery.cjs'` 或其他主进程模块缺失错误，进入画布后仍能显示恢复提示和日志保留选项。
- v0.6.3 画布恢复与日志保留：在 v0.6.1 完整升级备份和 v0.6.2 当前数据副本上启动，画布库应提示可恢复项目；确认后保留当前项目、补回缺失项目，同 ID 采用较新的更新时间，节点、连线与缩略图可打开，AI/WebDAV 配置恢复为升级前值。验证迁移已完成的环境再次覆盖安装时 `Data cache`、配置和 Result 不被新备份重复替换。分别模拟 IndexedDB 拒绝读取、损坏 JSON 和存在 localStorage fallback，应用不得以空列表覆盖数据。运行日志切换 7/14/30 天后应立即清理过期 JSONL，格式损坏或无有效时间的诊断行保留；长时间运行次日自动清理，清空全部日志仍只在确认后执行。
- 画布 `@` 引用替换：在生成配置「组装提示词」、图片/视频/文本/音频节点提示词、展开编辑器及文本节点正文中，依次插入 3 个已连接资源；鼠标和 Enter 选择 `@` 或 `@中文查询词` 后，触发文本应被完整替换，不得残留 `@`、重复引用、乱序或丢失前后普通文字。微软拼音、搜狗和微信输入法组合输入同样验证。
- 参考图自动上传 R2：先部署本仓库更新后的 `cloudflare/r2-media-worker`，将 Worker 地址、随机 `UPLOAD_TOKEN` 和同一 Worker 的 HTTPS 公网地址填入「OSS设置」。在生图、视频工作台分别拖入单张 PNG、一次选择 5 张 JPG、拖入多张 WEBP 与 Ctrl+V 粘贴截图；每张应先出现本地预览、最多 3 张并发上传、成功后自动替换为 Worker HTTPS URL 并按 UI 顺序传给 Agnes/图像模型。验证 Worker 返回 `publicUrl`、`url`、仅 `key` 三种格式，Worker GET/HEAD 读取、错误令牌、不可访问 Worker、缺少任一配置、GIF/BMP、超过 100MB、上传中断、失败重试、移除、最近上传复用和手动 HTTPS URL 添加；不得显示令牌、`TypeError` 或 `.trim()` 错误。确认移除参考图不会删除 R2 对象，R2 Public Access 保持关闭。
- OSS设置 / Cloudflare R2 + Worker 托管：在自己的 R2 桶、Worker 和随机 `UPLOAD_TOKEN` 上部署仓库模板，使用自定义 HTTPS 域名分别上传本地图片、MP4/MOV 和 MP3/WAV；Agnes 2.5 的画布首次生成、重试和视频工作台均应收到公网 URL。验证错误令牌、错误 Worker 地址、非 HTTPS 域名、超过 100MB、取消上传和 Worker 返回异常时的可恢复提示；再覆盖无配置、旧版阿里云配置、仅含 R2 Provider、缺失/`null` 字段、保存后重开和清除配置，页面不得崩溃；`r2.dev` 仅作为开发验证，不作长期生产地址。
- 画布主题圆形扩散：在 Windows 已开启和关闭“动画效果/减少动态效果”两种环境中，从画布外观面板点击浅色、深色以及顶部主题按钮；新主题都应从实际点击按钮中心圆形展开，不得整页闪变、重影或卡在半屏。连续切换、100%/125%/150% 缩放与重启后的主题持久化同样验证。
- Agnes Video 2.5 / 2.5 Flash：使用真实但受控的 API Key 分别验证 2.5 的 text、1–2 张 keyframe、3 张以上或含音/视频的 reference 请求，以及 Flash 的 720P、最多 5 图、拒绝参考视频；画布首次生成、失败重试、节点模型覆盖和视频工作台应保存并回显时长、清晰度、比例、种子等参数。验证本地图片/音频/视频通过用户已配置的签名 OSS 托管，未配置或上传失败/取消时有明确提示。此项未在自动测试中调用可能计费的真实生成接口。

- Agent Core 0.2.0：从空插件目录安装后，确认服务归档校验、解压与启动都在 staging 完成；故意损坏归档、树清单和运行时入口时应回滚旧版本。用真实 Codex Runtime 验证 `/health`、`/config`、MCP initialize 与 `tools/list` 的 34 个工具；浏览器/设备码/API Key 登录、登出、本地和 HTTPS 远程 Agent、断线恢复仍待人工验收。
- 画布引用输入（优先微信输入法）：在「组装提示词」和图片/文本/video/audio 引用 chip 输入框中，覆盖组合最终 input 位于 compositionend 前后、`@中文` 选中、两个 chip 之间输入、Backspace/Delete、失焦/关闭/缩放卸载及外部 value 更新；不得残留首字母、重复、乱序或改变 token 顺序。微软拼音和搜狗同样回归。

- 画布批次根节点重试布局：分别制造短/长错误正文，在 100% 与非 100% 缩放、明暗主题、最小节点尺寸及展开/收起状态下确认「一键重试」与加载态内容始终在节点内容区水平、垂直居中；成功图、叠卡溢出和展开动画不受影响。
- 画布 `@` 中文输入：在节点提示词、展开编辑器、文本正文与生成配置组装提示词中，分别使用微软拼音、搜狗和微信输入法，在 `@` 后输入、选词、Enter、空格、方向键、退格及插入引用后继续输入；候选键不得触发引用菜单、提交或关闭，受控文本不得回退旧值。
- 画布并行生成操作：图片、视频、音频、文本、生成配置及复用内置面板的插件节点连续点击两次，并让两个不同节点同时生成；左侧生成按钮持续可用，右侧方形停止按钮只在有任务时出现。停止其中一个发起节点应取消它的全部未完成任务、保留已完成结果且不影响另一个节点；单独取消批量子槽位不影响兄弟槽位。空节点首次原位生成、运行中再次生成创建独立结果；`writeBackToSelf` 插件再次生成应终止旧任务且旧结果不得迟到回写。
- 功能插件中心：在线、离线、代理、哈希失败、下载取消、磁盘不足、旧版本回滚和“修复”状态均有明确提示；只在打开插件中心或手动刷新时请求清单，未经点击不得下载插件、更新或 Codex 运行时。节点插件的官方/第三方 URL 安装、启停和更新行为保持不变。
- Agent Core 运行时：分别验证可用 PATH Codex、WindowsApps 拒绝访问、超时、版本不符、手动选择 `.exe`/`.cmd`、缺失时下载受管运行时及应用重启后的重新探测；复用的系统 Codex 绝不被修改或删除，卸载 Agent Core 后仅删除无引用的受管运行时。
- Agent 与 Skill：安装 Agent Core 后确认右侧面板可聊天、查看历史、选择模型/推理强度、上传附件、处理权限确认、读写当前画布、使用 MCP、连接 HTTPS 远程 Agent；安装 Skill 管理后确认列表、创建、编辑、启停、删除、从对话/画布生成草稿及冲突提示。禁用/卸载保留对话、凭据和 Skills 数据，卸载 Agent Core 会提示并同步移除 Skill 管理程序。

- 精修工作台（参考图重构）：空状态点击/拖入/Ctrl+V 粘贴/剪贴板按钮/我的资产载入图片；GIF 等非 JPG/PNG/WebP 格式与超过 50MB 的图片在读取前提示且不载入；已有图片时载入新图弹出替换确认。
- 精修预览区：滚轮围绕指针缩放，比例按图片实际像素显示且上限 800%；+/- 按钮、适应窗口、实际像素 1:1、左键拖动平移、中键平移与空格临时抓手；裁切或替换图片后视图自动回到适应；缩放/平移/对比位置重开软件后不写入草稿。
- 精修原图与对比：「原图」显示相同裁切与几何变换下未应用滤镜/调色/LUT 的图像；「对比」分割线可鼠标拖动与左右方向键调节（role=slider），左侧原图/右侧效果图标注正确；Escape 关闭原图与对比；关闭时释放渲染缓存。
- 精修四页签：页签方向键/Home/End 切换与焦点环；「裁切与变换」比例预设、自定义裁切（确认后主页面比例与弹窗一致）、±90 度旋转、翻转、精细旋转；「滤镜与调色」八种滤镜、单项/整组重置、LUT 导入/强度/移除；「AI 工具」模式切换、补充要求与模型能力提示；「导出设置」分辨率至 8K、自定义宽高联动与超限提示、格式与质量。
- 精修历史：底部撤销/重做覆盖旋转、翻转、滤镜、调色与 LUT；滑杆拖动过程即时预览、松手只产生一条历史；撤销后提交新编辑，重做分支被正确截断；旧会话草稿（无 future 字段）恢复后仍可撤销且不可重做。
- 精修导出与发送：导出图片/发送到生图/发送到视频与既有链路一致；无图或尺寸超限时按钮禁用；会话草稿（含历史与重做链）500ms 防抖落盘，切换页面与退出应用后完整恢复。
- 精修布局适配：1000×700、1360×900、1920×1080 与 100%/125%/150% 缩放、明暗主题下，预览区/侧栏/底栏不遮挡错位；窄窗口纵向排列、底栏换行、侧栏内部滚动正常。

- 画布节点缩放：图片、文本、视频、配置、组和插件节点从四角缩放时，节点编辑面板和右键工具栏仅在拖动期间隐藏；松开鼠标、窗口失焦或节点被删除后立即恢复，不影响尺寸、连线、选区、组固定、批量图和撤销重做。
- 画布提示词展开编辑：图片和文本已有内容节点在行内或「展开编辑器」修改提示词后，关闭面板、重开项目和重启软件仍保留草稿；行内与弹窗输入实时同步，镜头、提示词优化、模型设置和 @ 素材引用保持可用。
- 画布文本生成数量：新建配置节点文本模式默认生成 1 个结果；在文本设置中改为 2–15 后按数量创建结果节点；旧画布配置节点未保存 `textCount` 时继续使用原 `count`，普通文本节点未设置时仍生成 1 个结果，图片数量不受影响。
- 网络错误提示：模拟无响应的网络、DNS、TLS、错误 Base URL 或浏览器跨域失败时显示排查说明；401/403、429、上游错误正文和取消生成仍保留原有提示语义。
- 提示词源 Freestylefly GPT Image 2：升级后默认关闭且不自动下载；手动启用后可正常拉取和缓存，已有内置来源启用状态与自定义来源保持不变。
- 生图工作台进行中详情：同批生成多张图片时，任一先完成图片右键「查看详情」应立即打开，显示该批提示词、参考图、参数、请求张数与当前成功/失败/取消/进行中统计；最后一张结束后详情自动成为正式记录。
- 画布中文输入：分别在文本节点正文、节点提示词、展开编辑器、配置节点组装提示词、节点标题、画布标题、项目标题、侧栏搜索、新增文本资产、提示词库搜索、蒙版提示词、自定义尺寸和插件 URL 中，使用微软拼音、搜狗和微信输入法输入并选词；首字母不得提前上屏或被回退为英文，候选词、退格、空格、Enter、换行、@ 引用、失焦重开和画布快捷键均正常，100% 与非 100% 缩放下表现一致。

- 镜头隐藏发送：在生图/视频工作台、画布图片/视频节点和生成配置节点选择镜头后，提示词输入框不出现镜头文字；实际生成请求应携带按“视角→景别→距离→镜头→机位”排列的描述。“自动”不发送该组；切换页面、重开工作台、查看/恢复记录和重试时选择保持一致。
- 画布批量图片失败：左侧批量根节点显示「一键重试」，点击后仅重试失败子图，原成功图不重新生成；有新成功图时根节点预览更新，仍失败时显示上游原始错误。
- 画布反向连线：从节点右侧连接头向左拖到目标节点的左侧/右侧后，线连接到实际松手的节点和侧边；重叠节点不应误连，旧画布和插件创建的连接仍正常显示。
- 画布错误正文：分别触发图片、文本、视频、音频节点失败，节点和提示应显示上游返回的真实错误；仅无错误正文时才回退 HTTP 或本地错误。
- 画布断线子节点：批量生成多张图片后删除其中一条“批次根节点→子节点”连接线，再删除批次根节点；已断线子节点应继续保留，仍连接的批次子节点应随根节点删除。

- v0.4.8 紧急恢复：在 v0.4.7 已留下 `migration-v0.4.7.json=status: failed`、非空 `Data cache` 和 `Data cache.migrating-v0.4.7*` 的环境直接覆盖安装，确认只弹一次应用、自动从原 v0.4.7 校验备份恢复，画布/设置/素材/历史/Result 全部可读，旧目录不被静默删除。
- v0.4.8 并发恢复：同时启动两个实例时仅持有单实例锁的进程执行迁移；目标目录在原子切换前被创建为空目录时自动重试，已是完整快照时补写完成标记，不再出现 `ENOTEMPTY` 或第二个 JavaScript 主进程错误框。
- 更新可见安装：点击「重启并安装」后显示「正在保存并打开安装程序…」；15 秒内没有持久化回执则恢复按钮并显示错误，迟到回执不能再触发安装。正常回执应退出并弹出可见的 NSIS 安装向导，预填当前程序安装目录；完成页可重新运行新版本，`app-data/update-install.log` 记录请求、回执与启动结果。
- v0.4.7 覆盖安装/自动更新：升级前先按日志提醒人工备份；安装器应在 `%LOCALAPPDATA%\LY Space\Backups\v0.4.7-*` 生成“当前安装数据”和“旧 AppData”两份快照及 SHA-256 清单，失败时中止安装且旧数据不变。
- v0.4.7 数据迁移：升级后画布、连线、视口、渠道/API 设置、主题、提示词源、插件、素材、媒体 Blob、工作台会话、生成历史与参考图交接均可读取；默认缓存位于 `%APPDATA%\LY Space\Data cache`，生成结果位于“文档\LY Space\Result”，旧 AppData 不与当前 LevelDB 混合。
- 自定义存储保护：用户设置过的缓存和结果目录（含历史样式、跨盘及安装目录内自定义子目录）再次覆盖安装后保持原路径、不移动数据。分别在 `Data cache`、`Result\Picture`、`Video`、`Audio`、`text` 与 `app-data` 放置多层哨兵文件并记录 SHA-256，升级前后逐文件哈希必须一致；配置损坏或存储盘不可访问时程序应停止启动，配置文件和原数据不得被改写或改用空白默认目录。
- GRS AI 参考图：分别用 `gpt-image-2` 与 `nano-banana-2` 做一次真实参考图生成；其余 `gpt-image-2-vip`、`nano-banana-2-lite`、`nano-banana-fast`、`nano-banana-pro` 核对请求体，确认使用新版 `images` 且本地图片为裸 Base64。
- 画布图片/视频节点编辑面板：优化提示模块（文本模型+优化按钮）与提示词库/模型/设置/镜头同行，位于生成按钮左侧；面板宽度自适应拉长，所有控件完整显示不挤压。
- 画布组节点：把任意节点拖进组框架内（无需手动归属），拖动组时该节点跟随移动，松手后自动成为组内节点；「固定」钉子固定后，位置框在组框架内的所有节点均不可移动（含未归属的）。
- 画布组节点右上角：「固定」钉子按钮与「N 个节点」标签位于右上角，组图标与「组」名称在左侧。
- 画布组节点「固定」按钮：点击钉子后组及组内所有节点无法拖动（后续拖入组内的节点同样被固定），再点击取消；固定的组仍可选中/删除/调整帧大小，重新打开项目后固定状态保留。
- 画布复制节点：选中批量图片组根节点后 Alt 拖拽复制或 Ctrl+C/V 粘贴，只复制根节点本身（不再连带后方子图，副本显示为单图）；副本自动带左侧输入连线（连线指向原源头节点，不复制源头节点）；Alt 单击未拖动仍可撤销副本。
- 画布图片详情弹窗：任意缩放级别（缩小到 20% 也可）按住图片拖动平移查看，重置按钮恢复居中。
- 画布图片/视频节点编辑面板：提示词工具行（提示词库/文本模型/优化）在上左对齐，生成工具栏（模型/设置/镜头/生成）在下独立，不再重叠冲突；文本/音频节点面板保持原布局。
- 生图/视频工作台设置区「模型/镜头」行：模型下拉占满剩余宽度，长模型名完整显示不截断；镜头按钮紧凑靠右，移动端参数抽屉同样生效。
- 画布生成中的节点：图片、视频、文本节点右键工具栏仅在存在实际请求时显示「取消生成」；点击图片批量中的一个子节点后仅该节点恢复空闲，其他槽位继续。源节点编辑面板的「停止」仍停止整批任务；所有槽位取消后汇总节点不得误显示为生成失败。
- 画布图片节点右键工具栏：已完成图片固定显示「复制图片」，复制的是本地原始分辨率图片而非缩略图；PNG、JPEG、WebP 与透明图片可粘贴到系统画图及其他支持图片粘贴的软件。
- 设置“运行日志”：桌面版设置中可查看系统、网络/API、错误和关键操作；按分类、级别和关键词筛选结果正确，复制与导出内容为当前筛选结果，“打开目录”可定位 `app-data/app.log`，清空后列表与文件同步为空。填写 API Key、WebDAV 密码或发起带鉴权请求后，日志不得出现密钥、密码、令牌、Authorization 或请求正文。
- 生成配置节点「组装提示词」编辑框：镜头改为「镜头」按钮+弹层展开（与其他面板一致），点击展开 5 组选项，插入/移除提示词逻辑不变。
- 画布库项目卡片可拖动自定义排序：目标卡片左半区插入前、右半区插入后，蓝色插入指示线与松手后的最终顺序一致；两列、三列、跨行移动和重开软件后顺序均正确，新建画布仍出现在最前。
- 删除生图工作台生成记录：若该图曾「添加到资产」并插入画布，删除记录后资产与画布中的图片应保持正常显示，关闭/重开项目后仍正常（不再黑块）。
- 画布图片节点加载失败降级：图片内容无法加载时显示「图片加载失败」占位而非黑块；重新编辑/替换节点内容可恢复正常显示。
- 宽高比自动：生图尺寸选择「自动」时，Agnes 渠道应不再固定输出 1:1 正方形，而是由接口按提示词自动决定宽高比（openai/gemini/grsai 渠道同步验证）。
- 取消生成：生成中的图片右键「取消生成」后，该卡片直接从结果区消失（其余槽位继续），生成记录的取消数准确。
- 结果图勾选批量操作：每张结果图左上角复选框可勾选；勾选后标题栏出现「已选 N 张 / 下载选中 / 添加资产 / 删除选中」；「全选」对所有成功结果图生效；下载选中逐张弹保存对话框。
- 保存/按钮稳定性：生成结果（含远程 http 图片）保存记录、加入参考图、添加到资产、下载均不再报「保存失败 / 加入参考图失败 / 添加到资产失败」；如仍报错请记录完整错误文案。
- 结果详情与操作：生成图片右键「查看详情」在单张完成后立即可用（同批仍生成时显示实时统计，含无参考图时显示「暂无参考图」）；「下载 / 加入参考图 / 添加到资产」失败时均有明确提示；生成结果自动落盘到设置页显示的 Result 对应文件夹。
- 生图工作台逻辑修复：查看历史记录不再改动生成参数（模型/质量/尺寸保持当前设置）；参考图最多 6 张、超限时提示并截取；删除记录时参考图缓存一并清理；生成完成但记录保存失败时会弹提示而非静默丢失。
- 生图工作台性能与恢复：生成过程中不再每秒写入会话（长任务时不卡顿），生成结束后重开软件仍能恢复最近一次的结果、输入与参考图。
- 生图尺寸与模型行为：3:1 比例搭配 1K 分辨率可正常生成；尺寸超限提示与实际限制一致；Gemini 模型手动填写像素尺寸（如 2048x2048）按该尺寸生成；使用带脚本的模型时选择蒙版编辑会提示不支持。
- 文本问答兼容性：使用忽略 stream 参数、直接返回 JSON 的 OpenAI 兼容代理时，问答仍能正常返回内容；SSE 中出现 ping 等非 JSON 行不再中断输出。
- 画布文本节点流式输出：使用 GRS / OpenAI 兼容 / Gemini 渠道生成文本节点时，流式内容应逐段累积完整显示，而不是只显示末尾片段（含插件 onDelta 回调）。
- 落盘提示：生成结果若为远程图片 URL 且拉取失败（或本地目录写入失败），应弹出全局存储错误提示。
- 生成结果保存目录：生成图片/视频/文本/音频后应立即自动保存到“文档\LY Space\Result”下的 Picture/Video/text/Audio 四个文件夹（按类型对应）；配置弹窗「结果保存目录」仍可修改并复制历史文件。
- 生图工作台结果右键：图片右击「查看详情」弹窗展示该图对应的提示词、参考图（可点大图）、模型/尺寸/质量/分辨率/耗时/大小等信息；右键「下载」同样走保存对话框。
- 生图工作台结果常驻：生成完成后关闭软件重开、或从生图工作台切到其他模块再切回，结果区图片、提示词与参考图均保留且可正常显示。
- 生图工作台结果区操作：「全选 / 删除 / 清除失败」按钮位于生成结果标题行，删除确认弹窗正常；左侧已无生成记录模块，无「新建 / 切换记录」入口。
- Agnes Image 2.0/2.1：文生图与带参考图编辑请求不应包含 `response_format`；应使用明确像素尺寸、顶层 `return_base64` 与 `image` 数组。1K、2K、4K 和常用比例应正确出图，8K 应明确提示不受支持。
- 生图取消：同时生成多张图片时，右键单个“生成中”槽位并选择“取消生成”，其余槽位应继续；取消卡片应可重试，记录中的成功、失败、取消数应准确，全取消后可立即再次生成。
- 响应式结果卡：全屏、普通非全屏及更窄窗口下，“添加到资产 / 加入参考图 / 下载”均应完整显示；空间不足时操作栏应自动换为两行且不溢出。
- 画布与节点插件：画布的节点、连线、撤销重做、生成流程和插件 `applyOps` 应继续正常工作；桌面启动后不得出现 Codex/Agent 面板、后台 Agent/Codex 进程或 17371 端口连接。
- 桌面打包：运行打包后，校验脚本应确认安装包不超过 v0.5.6 基线加 3 MiB，且 `resources` 中不含 `canvas-agent`、`codex.exe` 或功能插件产物。
