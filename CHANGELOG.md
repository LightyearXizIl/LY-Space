# CHANGELOG

## Unreleased

## v0.4.4 - 2026-08-10

+ [修复] 删除生图工作台记录时保留仍被资产/画布节点引用的图片（共享 storageKey 不再直接回收），修复关闭/重开项目后画布图片偶发失效为黑块。
+ [优化] 画布图片节点加载失败自动降级：缩略图失败回退原图、原图失败按 storageKey 从本地缓存重新解析，仍无法恢复时显示「图片加载失败」占位（不再显示黑块）。
+ [新增] 画布库项目卡片支持拖动自定义排序：按住卡片拖到目标卡片前/后松手生效，顺序持久化保存（重开软件后保留）。

## v0.4.3 - 2026-08-06

+ [优化] 生图/视频工作台「模型」与「镜头」分列设置区左右两列：模型下拉独占左列（空间更足），镜头模块移入右列与模型同排（原右列为空白），移动端参数抽屉继续左右并排。

## v0.4.2 - 2026-08-06

+ [优化] 画布资产侧栏类型筛选标签：选中态改为柔和样式（浅色/暗色适配，不再黑底白字），全部/图片/视频/文本/音频 5 个标签一行排布不折行（侧栏极窄时横向滚动）。
+ [优化] 生图/视频工作台设置区「模型/镜头」行：模型下拉占满剩余宽度（长模型名不再截断），镜头按钮紧凑靠右，按钮样式与模型下拉统一（同高圆角胶囊、透明底 + 边框）。

## v0.4.1 - 2026-08-06

+ [修复] 画布节点工具栏仅在右键节点时弹出（移除"选中节点即显示"的残留逻辑）。
+ [新增] 图片预览放大后可按住拖动平移查看（缩放 0.2x–5x 保留，重置同时恢复位置）。
+ [新增] 画布资产侧栏搜索框下新增类型筛选（全部/图片/视频/文本/音频），与标签筛选/搜索共存。
+ [优化] 镜头弹层按钮换行不再越过分组标题（标签固定列 + 按钮独立换行区）。
+ [优化] 生图/视频工作台设置区「模型」与「镜头」左右分列，镜头标题与模型标题同高对齐。

## v0.4.0 - 2026-08-05

+ [新增] 画布右键节点弹出工具栏（竖向两列），节点/选中不再自动显示工具栏；右键节点同时单选该节点。
+ [新增] 画布 Alt+左键拖拽复制节点（框选组同样生效，组/批次引用与连线一并复制）；Alt 单击不拖动不产生副本。
+ [新增] 画布空白处右键打开「添加节点」弹窗（原双击左键创建迁移至此）；空格键按下立即切换抓手光标（配合左键平移）。
+ [新增] 画布图片预览支持鼠标滚轮缩放（0.2x–5x，含比例显示与重置）。
+ [新增] 画布资产侧栏显示全部类型（新增音频分组）；添加菜单支持本地上传（含音频）/从画布添加/新增文本。
+ [优化] 镜头模块改为按钮+点击展开：画布节点面板在图像/视频设置右侧、生图/视频工作台在模型下拉右侧；选项按钮选中态适配明暗模式（不再整块反色）。
+ [优化] 画布提示词优化可选择文本模型（节点级记忆，生成配置节点同步支持）。
+ [调整] 移除画布节点右键菜单（复制/删除，删除仍可用 Delete 键，复制可用 Alt 拖拽/Ctrl+C）；移除空白双击创建节点；快捷键提示同步更新。

## v0.3.9 - 2026-08-05

+ [新增] 生图工作台、视频创作台与画布节点新增「镜头」模块：视角/景别/距离/镜头/机位五组选项，点击把构图描述插入提示词（再点移除、组内单选），辅助控制构图与视角。
+ [新增] 画布节点编辑框（图片/视频节点与生成配置节点）新增提示词优化：一键调用文本模型流式优化提示词；生成配置节点的优化保留 @ 资产引用标记。
+ [修复] 关于页更新提示（Alert）在浅色模式下的深灰背景：info 色不再跟随黑色主题色，恢复默认蓝色系（浅蓝背景 + 蓝色图标）。

## v0.3.8 - 2026-08-05

+ [优化] 画布鼠标空闲时恢复系统默认指针（不再显示抓手）。
+ [优化] 节点编辑框与上方工具栏仅在选中节点时出现，框选空白/取消选择立即收起。
+ [优化] 画布连线拖拽更跟手（同步更新），连接失败有明确提示（不能连自己/连接已存在/配置节点不可连）。
+ [优化] 窗口标题栏跟随应用浅色/暗色主题切换。
+ [说明] 更新升级不删除用户数据：安装目录的生成结果与缓存由安装脚本保护，画布/渠道/会话等数据位于系统用户目录天然保留。

## v0.3.7 - 2026-08-05

+ [新增] 画布鼠标左键框选：左键按住空白处拖动即可框选多个节点，点击空白取消选择，Shift+左键追加框选；画布平移改为中键或空格+左键拖动。
+ [调整] 新建渠道草稿化：点「新增渠道」不点保存不会写入配置，直接关闭即丢弃，避免残留空渠道。
+ [调整] 默认渠道改为 OpenAI（新安装/重置配置生效）：默认连接 api.openai.com，不含任何预置密钥，全部由用户自行填写。

## v0.3.6 - 2026-08-05

+ [新增] 更新下载支持断点续传：暂停下载保留已下载进度，继续从断点接着下（网络中断不再从头开始）；主进程自研下载器（Range 续传 + sha512 校验），完成校验通过才安装。
+ [新增] 关闭按钮最小化到系统托盘：点右上角 X 只隐藏到右下角任务栏托盘，不退出；托盘左键单击恢复主界面，右键菜单可「显示主界面」或「关闭」（关闭前自动保存数据）。
+ [优化] 更新弹窗居中显示在画面正中心，下载全程驻留（暂停/失败不消失）；下载中可「暂停」，暂停后可「继续下载」，失败显示错误可「重试」。
+ [优化] 关于页版本更新支持暂停/继续下载，下载状态与进度实时显示。
+ [优化] 更新安装改为静默覆盖升级（NSIS 静默安装到当前目录，安装完成后自动重启应用）。

## v0.3.5 - 2026-08-04

+ [优化] 整体流畅度大提升：画布拖拽/缩放/框选/连线全部 rAF 合并 + 持久化 500ms 防抖（不再每帧全量写库）；缩放不再重渲染节点（scale 移出 props，经轻量 viewport store 读取）；连线/顶栏/工具栏/缩放控件/侧边栏 memo 化，平移缩放不再全量重渲染。
+ [优化] 图片缩略图管线：画布节点与素材封面默认渲染最长边 1024 的降采样图（LRU 缓存 + 并发限流），大图显存占用大幅下降；画布放大超过缩略图能力自动切回原图保证清晰。
+ [优化] 提示词库虚拟滚动：按窗口断点分列，只渲染视口附近的行，无限滚动加载再多卡片也不累积 DOM。
+ [优化] 视频创作台生成期间不再每秒全量写会话到 IndexedDB（对齐生图工作台）；轮询在页面切走后只写库不更新 UI。
+ [优化] 精修工作台：调色/滤镜滑杆拖动不再每帧写入历史记录，松手才提交；预览重绘 rAF 合并；会话保存防抖。
+ [优化] 配置弹窗/设置面板输入框改为本地草稿 + 失焦提交，输入不再每键触发全局配置序列化与页面重渲染。
+ [优化] 素材页封面懒加载异步解码；提示词卡片 memo 化（滚动加载时未变化卡片不重渲染）。
+ [调整] 画布节点悬浮工具条自动换行：图片节点工具全开时超过窗口宽度自动折为两行。

## v0.3.4 - 2026-08-04

+ [新增] 我的资产支持音频：素材页可上传/预览/下载音频，画布音频节点可一键加入资产，资产选择器可复制或插入音频（插入到画布）。
+ [新增] 配置对话框新增「阿里云设置」Tab（位于「关于」左侧）：OSS 托管配置与公网参考图 URL 管理；视频创作台参考图的「OSS 设置」「添加公网 URL」入口迁移至此。
+ [调整] 资产选择器：文本资产未设置图片时直接显示内容，排版更合理。
+ [调整] 关于页去除「最新版本」显示，仅保留当前版本；修复更新完成后软件内更新日志不显示的问题。
+ [修复] 视频创作台选择 Agnes 模型后设置面板未生效（渠道判断被全局默认模型误导）。

## v0.3.3 - 2026-08-04

+ [新增] 资产选择器：资产卡片新增「复制」与「插入」按钮，移除鼠标悬浮插入。
+ [新增] 启动时自动检查更新并居中弹窗提醒：弹窗内可查看版本号、更新日志并直接下载（含下载进度、重启安装）；关于页手动检查更新不弹窗。
+ [调整] 关于页更新日志默认显示当前版本，检查到新版本后显示最新版本的更新日志。
+ [调整] 移除提示词库「个人词库」功能。

## v0.3.2 - 2026-08-04

+ [新增] 提示词库个人词库：本地保存个人提示词，可新增/编辑/删除，远程提示词可一键「加入我的词库」。
+ [新增] 配置页渠道启用/关闭开关：禁用后该渠道模型从各模型选择器中隐藏，请求自动回退到已启用渠道。
+ [调整] 生图/视频工作台参考图：移除「剪切板」按钮，鼠标移到参考图框内即可直接粘贴图片。
+ [调整] 检查更新交互：点击后不再弹窗、不自动下载；发现新版本后在「关于」页手动点击下载，并显示该版本更新日志。

## v0.3.1 - 2026-08-03

+ [修复] Agnes 视频按官方文档适配：结果查询路径正确剥离 /v1（GET /agnesapi），创建任务 /v1/videos 由代码自动补齐——中国站（api.agnes-ai.cn）与国际站（apihub.agnes-ai.com）均可正确工作。
+ [优化] 错误提示增强：响应为 HTML 时提示「Base URL 可能填成了网页地址，请改为 API 地址」；Agnes 服务端异常（litellm/UploadFile）时明确提示可能正在维护。
## v0.3.0 - 2026-08-03

+ [修复] Agnes 渠道（含中国站 apihub.agnes-ai.cn）请求网络层失败导致"任务创建失败"：新增主进程请求代理，axios 网络层失败（CORS/连接/超时）时自动回退主进程重试；失败错误显示真实原因（Network Error 等），便于定位。
## v0.2.9 - 2026-08-03

+ [修复] 视频参考图 base64 直发被 Agnes 拒绝导致"任务创建失败"：回退该方案；图床上传前统一压缩（最长边 1024、JPEG 压缩）提高上传与下载成功率，托管失败时明确提示需公网可访问地址。
+ [优化] 生成失败错误信息增强：Agnes 真实响应与未知异常会直接展示，便于定位问题。
## v0.2.8 - 2026-08-03

+ [修复] 视频参考图上传后 Agnes 报 "image URL could not be downloaded"：图床优先级调整为 uguu.se 优先（tmpfiles.org/catbox.moe 兜底），上传前统一转 PNG/JPEG 保证格式兼容；图床全部失败时回退以 base64 直发，兼容 Agnes 中国站（国内服务器访问海外图床受限）。
## v0.2.7 - 2026-08-03

+ [修复] 视频参考图自动上传报 "Failed to fetch" 彻底解决：免费图床改为 tmpfiles.org 优先、catbox.moe 兜底，浏览器直传与主进程代理双通道，全部失败时明确提示配置 OSS 或改用公网 URL。
+ [调整] 视频创作台参考图移除「托管 OSS」手动按钮（生成时已自动托管，无需手动操作）。
+ [调整] 生图工作台宽高比默认改为「自动」（不传尺寸，由接口按提示词决定）。
+ [调整] 存储设置以用户设置地址为准：仅首次安装默认跟随安装目录，用户设置过的结果/缓存目录（含历史 E 盘目录）不再被强制改回。
+ [优化] 图片下载/批量下载保存对话框默认沿用上次保存的目录（跨重启记忆）。
## v0.2.6 - 2026-08-03

+ [修复] 视频参考图自动上传免费图床报 "Failed to fetch"：浏览器直传被跨域（CORS）拦截时自动回退主进程代理上传，Agnes 图生视频不再因上传失败中断。
## v0.2.5 - 2026-08-03

+ [修复] 生图工作台删除选中结果图时不再误删同批次其它图片：只删除勾选的图片，同记录其余图片与本地文件保留。
+ [新增] 视频创作台参考图自动上传公网：未配置阿里云 OSS 时自动改用免费图床（catbox）转公网 HTTPS URL，Agnes 图生视频不再提示"请先添加公网图片 URL"。
+ [修复] 视频生成结果本地同步保存：跨域结果 URL 自动回退主进程下载，确保保存到 Result\Video。
+ [修复] 删除生成结果图时本地文件同步删除更可靠：放宽路径校验（存储目录变更后不再误拒）、删除失败自动重试、删除结果明确提示。
+ [新增] 视频创作台提示词优化：文本模型选择 + AI 流式优化回填；参考图支持点击放大预览。
## v0.2.4 - 2026-08-03

+ [修复] 视频创作台生成视频不再提示 "video status query rate limit exceeded"：Agnes 状态查询遇到限流时自动跳过等待下一轮（不再中断生成），查询间隔由 2.5 秒调整为 10 秒。
+ [调整] 生成中的图片恢复为仅右击「取消生成」，移除鼠标悬停/点击卡片取消的交互。
## v0.2.3 - 2026-08-03

+ [新增] 软件启动时自动检测更新：发现新版本弹出提示框，可一键下载更新；下载完成后提示重启并安装。
+ [修复] 生成中的图片点击卡片即可取消生成（悬停显示「点击取消」提示），任何状态取消都有明确反馈，不再出现"点了没反应"。
+ [优化] 「优化提示词」按钮与文本模型选择器移出提示词输入框，改为在输入框下方独立排列，不再遮挡输入内容。
+ [修复] 视频创作台使用 Agnes 渠道时，本地参考图自动上传 OSS 转为公网 HTTPS 链接后再提交任务；未配置 OSS 时明确引导设置，不再直接报"请先添加公网图片 URL"。
## v0.2.2 - 2026-08-03

+ [新增] 提示词输入框右下角新增「优化提示词」按钮：调用所选文本模型（默认 gpt-5.5，按钮旁下拉可选择渠道 API 提供的文本模型）流式优化当前提示词并自动回填输入框，优化中按钮显示加载状态。
+ [修复] 参考图左右排序按钮在深色主题下白底刺眼，已适配深色半透明背景与浅色图标样式。
+ [优化] 关于页面「重启并安装」按钮改为与「检查更新」一致的 antd 按钮风格；版本更新日志只显示最新版本，不再滚动展示历史版本列表。
+ [优化] 切换页面不再中断正在生成的任务：生图工作台的生成任务改为模块级后台运行，切换页面后继续生成、返回页面自动恢复进度与等待计时，取消/清除失败仍即时生效，仅应用重启才标记任务中断；视频创作台任务轮询改为全局防重复，切页期间完成或失败的任务返回页面时自动同步到结果区。
## v0.2.1 - 2026-08-03

+ [修复] 生成结果右键「查看详情」「重新编辑」不再提示"未找到该图片的生成信息"：日志异步加载未就绪或跨重启恢复时自动重读本地记录兜底。
+ [修复] 删除生成结果图时同步删除本地文件：删除失败（文件被占用等）会明确提示，旧版本生成的图片无本地文件记录时提示已跳过。
+ [优化] 「下载选中」只弹一次保存对话框，全部选中图片统一保存到所选目录（重名自动加序号），不再逐张弹窗。
+ [新增] 生图工作台参考图支持点击放大预览（可左右切换）。
+ [新增] 提示词输入框右下角「优化提示词」按钮：调用 AI 流式优化当前提示词并自动回填。
## v0.2.0 - 2026-08-03

+ [新增] 生成结果右键「重新编辑」：将图片的生成操作（提示词、参考图、模型/尺寸/质量等参数）一键恢复到工作台，修改后可再次生成。
+ [修复] 生图工作台「清除失败」按钮失效：改为直接清除结果区中的失败卡片并同步清理失败生成记录，任何状态点击都有反馈。
+ [新增] 生成中「开始生成」按钮不再锁定，可随时追加提交新一轮生成请求（按钮显示为「追加生成」）。
+ [调整] 生成结果右上角按钮重构：「下载选中」「添加资产」常驻一级菜单（勾选图片后生效），「删除选中」并入最右「删除」按钮（勾选后可用并显示数量），「全选」保持全选/取消全选切换。
+ [新增] 删除生成结果图时同步删除本地已落盘文件（`Result\Picture` 等），本地文件不存在时自动忽略。
## v0.1.9 - 2026-08-03

+ [修复] 生图宽高比「自动」真正生效：Agnes 渠道不再默认出 1:1 正方形，选择「自动」时不传尺寸由接口按提示词决定宽高比。
+ [调整] 结果保存目录与缓存目录默认改为跟随安装目录（`安装目录\Result`、`安装目录\Data cache`），换目录安装自动迁移，E 盘旧数据自动迁回；升级/自动更新时安装目录内的数据受 NSIS 宏保护不再被清空。

## v0.1.8 - 2026-08-03

+ [修复] 数据不再随安装目录移动：`Data cache` 与 `Result` 固定保存在 E 盘，换目录安装/更新时不再复制到新安装目录；旧版遗留的浏览器数据自动并入 E 盘缓存目录。

## v0.1.7 - 2026-08-03

+ [修复] 生成结果自动落盘彻底修复：落盘链路同样支持主进程下载回退，远程图片不再出现"保存到本地目录失败"黄色提示，`Result\Picture` 等目录必定有文件。
+ [修复] 生成中的图片点击取消后直接从结果区消失（不再残留"已取消"卡片）。

## v0.1.6 - 2026-08-03

+ [修复] 生成结果保存彻底修复：远程图片 URL 下载失败（跨域等）时自动改用主进程下载，保存记录、加入参考图、添加到资产、下载不再报错。
+ [新增] 生图工作台每张生成结果图左上角可勾选，支持批量下载选中、批量添加资产、批量删除选中；「全选」改为对图片全选。

## v0.1.5 - 2026-08-03

+ [修复] 升级软件不再丢失生成结果：数据缓存（IndexedDB 生成记录/结果）默认迁移到 `E:\Software\LY Space\Data cache`（安装目录外，安装器清空目录不影响），旧缓存自动迁移；生成结果默认保存到 `E:\Software\LY Space\Result`。
+ [修复] 生图工作台生成结果右键详情在记录未保存时仍可查看；详情弹窗无参考图时显示「暂无参考图」；结果卡片「加入参考图 / 添加到资产」失败时给出明确提示。
+ [修复] 生图工作台提交新任务不再挤掉之前的生成结果，新结果自动排列在最前；旧缓存中损坏的 IndexedDB 数据迁移时自动跳过。

## v0.1.4 - 2026-08-03

+ [新增] 生图工作台生成结果支持右键查看详情（参考图、提示词、参数与图片信息），下载改为弹出保存位置选择对话框。
+ [调整] 生图工作台移除左侧生成记录模块与「切换记录」入口，全选/删除/清除失败移至生成结果区；生成结果跨重启与模块切换常驻保留。
+ [调整] 生成结果自动保存默认目录改为 `E:\Software\LY Space\Result`，图片/视频/文本/音频按类型分别保存到 Picture/Video/text/Audio 文件夹，旧默认目录中的历史文件自动迁移，仍可在配置中修改保存位置。

## v0.1.3 - 2026-08-03

+ [修复] 生图工作台查看历史记录不再改写全局生成参数；删除记录时同步清理参考图缓存；生成完成但记录保存失败时给出明确提示。
+ [优化] 生图工作台生成期间不再每秒全量保存会话（改为结果变化时保存），参考图数量上限 6 张并在超限时提示。
+ [修复] 图像尺寸校验提示与实际限制一致，3:1 比例搭配 1K 分辨率不再误报超限；Gemini 图片模型显式像素尺寸生效。
+ [修复] 文本流式问答兼容直接返回 JSON 的代理、忽略非 JSON 的 SSE 行；模型脚本使用蒙版时明确提示不支持。
+ [修复] 文本流式回调统一为增量语义，画布文本节点与插件流式输出不再出现只显示末尾片段的问题；Gemini 流式兼容直接返回 JSON 的代理。
+ [修复] 生成结果写入本地目录失败（含远程图片拉取失败）时通过全局提示告知用户。

## v0.1.2 - 2026-08-03

+ [优化] 版本更新「检查更新」「取消下载」按钮样式与配置弹窗其他按钮统一。

## v0.1.1 - 2026-08-02

+ [优化] 版本更新支持下载中取消下载，检查更新按钮文字加大更清晰。

## v0.1.0 - 2026-08-02

+ [优化] 软件所有数据跟随安装目录保存，更新或更换安装目录后自动迁移历史数据（生成记录、图片、缓存）。

## v0.0.9 - 2026-08-02

+ [修复] 生成记录改为保存在系统用户目录，更新或更换安装目录后记录不再丢失（旧数据自动迁移）。
+ [优化] 生成记录支持一键清除失败记录；关于页使用三角形 Logo，去掉微信号前多余符号。

## v0.0.8 - 2026-08-02

+ [新增] 配置弹窗新增「关于」页（作者与微信号、GitHub 链接、技术栈、检查更新与更新日志），右上角版本号点击进入关于页并提示更新。

## v0.0.7 - 2026-08-02

+ [新增] 生图工作台生成记录支持右键操作（查看详情、复制提示词、删除），记录详情展示提示词、参数、结果大图与保存目录。
+ [修复] 生图工作台切换记录不再挤掉当前生成任务，结果区与所选记录一一对应；重开软件后自动恢复上次查看的记录。
+ [优化] 生图工作台生成结果图缩小为自适应网格（全屏约 1/4 面积），精修工作台界面与生图/视频工作台风格统一、滤镜与调色项改为中文。

## v0.0.6 - 2026-08-02

+ [修复] 生图工作台提交新任务不再覆盖之前的生成结果，新任务自动排列在最前。
+ [修复] 生图工作台新建会话保留之前的生成结果，并支持在生成结果区切换查看历史记录。

## v0.0.5 - 2026-08-02

+ [修复] 兼容以 OpenAI 调用格式配置的 Agnes 图片渠道：生图请求不再携带不支持的 `response_format`，统一改用 `return_base64`。

## v0.0.4 - 2026-08-02

+ [修复] Agnes Image 2.x 改用原生 `return_base64`、顶层参考图和明确像素尺寸请求，移除不受支持的 `response_format`。
+ [新增] 生图结果槽位支持右键取消单张任务，记录区分成功、失败与取消。
+ [调整] 桌面版不再内置或启动 Codex/Canvas Agent；画布与节点插件继续使用通用 `applyOps`。
+ [优化] 窄窗口下生成结果操作按钮自动换行并完整显示文字；安装包校验上限收紧至 180 MiB。

## v0.0.3 - 2026-08-02

+ [新增] Windows 桌面端支持从 GitHub Release 检查、下载并在确认后自动安装新版本。
+ [调整] 版本更新仅展示 LY Space 正式版本，并按版本号倒序排列。
+ [修复] 内置 Canvas Agent 启动时不再弹出 Windows Terminal 黑窗。

## v0.0.2 - 2026-08-02

+ [优化] 桌面端改为独立渲染层与标准 Electron 打包，移除公开网页应用发布链路并显著缩小 Windows 安装包。
+ [修复] Windows 应用、安装程序、快捷方式、窗口和任务栏统一使用 LY Space 图标。

## v0.0.1 - 2026-08-02

+ [新增] 新增 Windows 桌面端与 NSIS 安装包，集成 LY Space 图标、中文菜单和本地 Canvas Agent。
+ [新增] 新增精修工作台，支持图片裁切、多档导出与一键发送到生图、视频创作台。
+ [新增] 支持 Agnes AI 官方 API、阿里云 OSS 参考图托管，以及结果与缓存目录配置。
+ [调整] 生图分辨率改为独立的 1K、2K、4K、8K 选项，桌面端不再限制参考图片文件大小。

继承项目的历史版本记录见 [UPSTREAM_CHANGELOG.md](UPSTREAM_CHANGELOG.md)。
