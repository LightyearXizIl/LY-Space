---
title: 新会话交接
description: 新 AI 会话接手前的快速上手指南（项目现状、关键决策与坑）
---

# 新会话交接文档

> 用途：新 AI 会话接手前的快速上手指南。代码优先遵守 `AGENTS.md`，本文补充项目现状、关键决策与坑。

## 0. 给 ZCode 的快速上手

- **项目地址**：本地 `E:\Vibe coding\LY Space\LY Space-源码`；Git 远程 `https://github.com/LightyearXizIl/LY-Space.git`（main 分支；当前发布版本 `v0.7.1`）。
- **最新发布状态**：`v0.7.1` 已于 `2026-09-05` 正式发布（非 draft/prerelease，GitHub latest 指向本版）；GitHub Actions `Publish LY Space Desktop` 运行 `33946112528` 成功。EXE、blockmap、`latest.yml` 均为 uploaded；本地留存文件与远端 digest、更新清单已核验，具体值见下方 v0.7.1 小节。
- **v0.6.7 发布内容与现场状态**：画布恢复仅由用户点击触发，异步扫描期间可关闭进度对话框且不会自动重开；诊断记录已脱敏的不可读来源错误。出问题电脑目前只显示 8 月 15 日以前的项目；实际 8 个来源是否可读及是否找到后续项目，仍须以该机运行日志和诊断导出为准，不能提前承诺。
- **接手顺序**：先读 `AGENTS.md`（开发规则）→ 本文档 → `docs/content/docs/progress/todo.mdx`（待办）→ `docs/content/docs/progress/pending-test.mdx`（待测）→ `CHANGELOG.md`（版本历史）
- **技术栈**：Electron（`desktop/main.cjs` + `desktop/preload.cjs`）+ Vite + React 19 + TS + antd 6 + Tailwind 4 + Zustand；通用 AI 接口仍可由渲染进程直连，方舟请求在桌面端通过受限主进程 HTTPS 代理；文档站是独立 Next.js 项目（`docs/`，bun）
- **启动/检查**：`pnpm --dir desktop run check`（类型检查）；`pnpm --dir desktop run pack:win`（打包到 `release/`）；文档站 `cd docs && bun run dev`
- **注意**：第 3.17 节是 Reasonix 环境专属技能（外部 `eyes.py` 看图工具），与本应用代码无关，ZCode 环境无此技能，可忽略
- **必守规则**（AGENTS.md）：最小改动、不重构、不改无关文件、不执行编译/构建（除非用户明确要求）、中文注释与文案；发版流程见第 5 节

## 1. 项目概况

- **桌面 AI 创作工作台**：无限画布、生图工作台、视频创作台、精修工作台、提示词库、我的素材、配置
- **技术栈**：Electron（`desktop/main.cjs` 主进程 + `desktop/preload.cjs`）+ Vite + React 19 + TS + antd 6 + Tailwind 4 + Zustand；方舟 API 经主进程受限 HTTPS 代理，其余兼容接口仍按渠道直连（无新增服务端）
- **代码结构**：
  - `desktop/main.cjs`：主进程（存储、IPC、自动更新、请求代理）
  - `desktop/preload.cjs` + `desktop/renderer/src/types/desktop.d.ts`：IPC 三端同步
  - `desktop/renderer/src/pages/`：image（生图）、video（视频）、canvas（画布）、refine（精修）、prompts（提示词库）、assets（我的素材）、config、home
  - `desktop/renderer/src/services/`：api/（各渠道请求）、image-storage、file-storage、desktop-storage（落盘）、oss-hosting、image-hosting（图床）、workbench-session
- **当前版本**：v0.7.1 已发布（GitHub Release、三项桌面资产和更新清单均已核验）。

## 2. 数据存储

| 项 | 默认位置 | 说明 |
|---|---|---|
| 生成结果文件 | `文档\LY Space\Result\`（Picture/Video/text/Audio 四文件夹） | 生成后立即自动落盘 |
| 数据缓存（IndexedDB/Local Storage） | `%APPDATA%\LY Space\Data cache\` | 画布、设置、素材、生成记录与媒体 Blob；`sessionData` 指向它 |
| 存储配置 | `%APPDATA%\LY Space\app-data\storage-settings.json` | 用户可改 resultRoot/cacheRoot |
| 上次下载目录 | `%APPDATA%\LY Space\app-data\last-save-directory.txt` | 下载对话框默认沿用上次目录 |

- **存储策略（v0.4.7 起）**：默认用户数据与程序文件彻底分离；用户自定义过的结果/缓存目录保持不变，换目录安装不覆盖用户设置。
- **首次升级保护**：v0.7.2 起使用安装阶段的 `customCheckAppRunning`（此前为 `customInit`），在自动保存退出后、旧卸载器运行前，把当前安装目录数据和旧 AppData 分别保存到 `%LOCALAPPDATA%\LY Space\Backups\v0.4.7-*`，逐文件 SHA-256 校验；新版只从当前安装数据快照恢复，旧 LevelDB 不合并，备份不自动删除。
- **v0.4.7 故障恢复**：若启动出现 `ENOTEMPTY` 或主进程 JavaScript 错误，先完全退出所有 LY Space 进程，再手动把 v0.4.8 覆盖安装到原目录；禁止删除 `Data cache`、`Data cache.migrating-v0.4.7*` 和 `%LOCALAPPDATA%\LY Space\Backups`。v0.4.8 会使用原校验备份重新恢复，并保留遗留迁移目录作为故障证据。
- **历史教训**：NSIS `CopyFiles "$INSTDIR\目录\*.*"` 不会递归复制子目录，旧 `customRemoveFiles` 随后执行 `RMDir /r` 会丢失嵌套数据。不得再把用户数据放回安装目录，也不得用通配符复制冒充递归备份。

## 3. 关键链路与已修复的坑

### v0.7.2：安装向导直接打开与自动保存退出（待 CI 发布核验）

- 安装器不再在 `.onInit` 等待已运行的软件退出；向导先显示，点击安装后通过 `customCheckAppRunning` 执行保存退出脚本，然后才备份数据和调用旧卸载器。覆盖默认强杀逻辑，失败/超时停止安装，不删除原数据。
- `stop-for-install.ps1` 仅定位同一 Windows 会话、同一完整 EXE 路径的进程，通过 `--lyspace-quit-for-install` 与 `--lyspace-install-dir=` 通知已运行的 Electron 主进程；最长等候 25 秒。主进程校验安装目录并复用带请求 ID、15 秒超时的持久化回执；通知进程若抢到单实例锁，只退出，不初始化存储或打开新窗口。
- 修复渲染层保存失败仍发送成功回执的问题；同步/异步状态保存与待写入失败均返回失败回执，主进程只接受主窗口发送者，失败保留运行中的应用。脚本保持 ASCII，避免 Windows PowerShell 5 系统代码页误读无 BOM 的中文注释而漏执行检查。
- 过渡限制：v0.7.1 及更早版本不识别退出通知，首次覆盖可能仍需“文件 → 退出”一次；该版本的新向导本身不会再在双击后等待旧应用关闭。从 v0.7.2 起支持后续自动保存退出，不使用强杀兼容旧版。
- 本地验证：完整 Node 38/38、Renderer Vitest 18 文件 103/103、TypeScript、主进程/preload 语法、版本一致性和 `git diff --check` 通过。隔离 C# 模拟进程验证保存后退出、空闲、精确路径隔离与超时不强杀；备份恢复测试覆盖 `Data cache` 嵌套目录、Result 四类文件、`app-data` 嵌套哨兵及逐文件哈希。
- 未操作用户实际 LY Space 进程或真实资料；完整 Electron/NSIS 窗口交互、托盘、卸载、不同权限账户和真实覆盖安装仍列入待测。TODO 已复核，无待实施条目需要迁移；`.mimosa/` 与 `desktop/.mimosa/` 保留且不提交。
- 发布提交、CI、正式资产 digest 和本地安装包路径在 CI 完成后单独补记。

### v0.7.1：工作台本地参考图不再强制 OSS（已发布）

- 背景：另一位用户的视频创作台添加本地参考图时被要求配置 R2；与此前画布的 `ModelNotOpen` 账号权限错误无关。
- `ReferenceImageUploader` 默认用 `uploadImage` 保存本机 Blob 和 storageKey，由既有 API 适配层转换文件/Base64。视频页只对无自定义脚本的 Agnes 视频开启公网托管模式；生图工作台同样默认使用本地参考图。手动 HTTPS 链接与既有最近上传记录保留；本地临时链接不提供“复制链接”。
- 渠道切换后清除不适用的托管警告，失败项重试使用当前模式。移除或离开页面后完成的任务不得重新加入参考图。清理图片缓存时保护视频生成记录及工作台会话引用。
- 回归覆盖无 OSS 本地添加、公网上传、渠道切换重试、处理中移除和清理保护；真实账号生成及另一台电脑的安装验收仍待用户完成。
- 验证：新增 6 项回归，Renderer Vitest 全量 100/100 通过，TypeScript 与 `git diff --check` 通过；TODO 的持续渠道兼容性事项继续保留。
- 发布提交/tag：`00211179219395264fbb7f5f8cd4bb4e78f932bb` / `v0.7.1`；`main` 和 tag 已推送。CI `https://github.com/LightyearXizIl/LY-Space/actions/runs/33946112528` 成功，完成类型检查、Windows 构建、包校验、草稿资产下载核验与正式发布。
- 正式 Release：`https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.7.1`，非 draft/prerelease，发布说明与 CHANGELOG 一致。三项资产均 uploaded：EXE 92,585,991 字节、SHA-256 `f6e2603bf25f5146fb51699b320891df8b983fa707eca0f29c24bb63000c9c66`；blockmap 97,911 字节、SHA-256 `432aa12a5ca59ca20f113ac6eebbb41863ec3d4ff3357ed3c723e8d76b764d90`；`latest.yml` 344 字节、SHA-256 `13640b524fce057c5695fede0816bfc841d4724fb85f7cf656a6be490ea4cc50`。
- 本地 CI 正式安装包：`E:\Vibe coding\LY Space\LY Space-源码\release\LY-Space-Setup-0.7.1.exe`，blockmap 在同目录；本版清单另存 `release/v0.7.1/latest.yml`，未覆盖旧清单。三份本地文件大小和 SHA-256 均与远端 digest 一致；清单 version/path/files/size/SHA-512 均匹配安装包，SHA-512 为 `6e61fiVVx6plv88KWn7boagVaX7VGvbD/WwG3fJdqGUm0fOUuFsQx9wZTSm9rW51TDIlB84qgBr2YRCHnQ6bNg==`。
- `.mimosa/` 与 `desktop/.mimosa/` 未修改或提交；本轮未进行真实收费调用或用户电脑安装测试。

### 3.1 图片生成与落盘（生图工作台 `pages/image/index.tsx`）
- **落盘**：API 返回 → `persistGeneratedImages`（`services/api/image.ts`）→ `fetchImageBlob`（浏览器 fetch 失败回退主进程 `lyspace:fetch-url`，绕过 CORS）→ `saveGeneratedBlob` → 主进程 `writeGeneratedOutput` 写文件并返回 `{ path, name }` → 图片记录 `localPath`
- **三条走 blob 的路径**（落盘/uploadImage 转存/下载）都必须用 `fetchImageBlob` 回退，别漏
- **删除同步本地**：`deleteSelectedImages` 只删除**勾选的图片**（v0.2.5 修复：不再误删同批次其他图），日志中移除被删图（无剩余才删记录），blob（storageKey）与本地文件（localPath）同步回收；主进程删除失败自动重试 3 次，结果有明确提示
- **切换页面不中断**（v0.2.2）：生成任务存模块级 `activeImageTasks`（Map + 订阅），组件卸载不 abort；返回页面时合并任务表恢复状态，仅真实重启才标 failed
- **结果图交互**：右键菜单（查看详情/重新编辑/下载）、左上角勾选（全选/下载选中/添加资产/删除选中，删除走最右「删除」按钮）、生成中卡片**仅右击取消生成**（v0.2.4 回退点击取消）

### 3.2 视频生成（视频创作台 `pages/video/index.tsx` + `services/api/video.ts`）
- **任务式生成**：`createVideoGenerationTask` 提交 → `pollVideoGenerationLog` 轮询（Seedance 5s / Agnes 10s / 其他 2.5s）→ `storeGeneratedVideo` 落盘 `Result\Video`（fetch 跨域失败回退主进程 `fetchUrl`）
- **轮询限流**：Agnes 状态查询遇 429/rate limit 返回 pending 继续轮询，不中断
- **切换页面**：轮询闭包后台继续，`activeVideoLogIds` 模块级防重复轮询，任务完成通知（`emitVideoTaskUpdate`）刷新新挂载页面
- **Agnes 参考图**（图生视频）：要求公网 HTTPS 图片 URL。视频工作台仅在无自定义脚本的 Agnes 渠道使用 Cloudflare R2 Worker 自动上传；其他视频渠道及生图工作台默认保存本地参考图，生成时交给各自 API 适配层转换。阿里云 OSS 的既有生成前托管仍保留；公网 URL 仍可手动添加，也可从「OSS设置」通过 `reference-handoff` 加入视频工作台。
- **Agnes API 路径（官方文档 agnes-video-v20）**：Base URL `https://api.agnes-ai.cn`（中国站）/ `https://apihub.agnes-ai.com`（国际站）；创建任务 `POST /v1/videos`（`buildApiUrl` 自动补 `/v1`）、结果查询 `GET /agnesapi?video_id=`（`pollAgnesVideoTask` 已剥 `/v1`）
- **请求代理回退**（v0.3.0）：`agnesRequest` 网络层失败（CORS/连接/超时）自动回退主进程 `lyspace:proxy-request` 重试；失败节点优先显示上游原始错误正文（包括 HTML、litellm/UploadFile 等），仅在上游没有错误正文时才回退 HTTP 状态或本地提示。

### 3.3 公共设施
- **IPC**：`lyspace:fetch-url`、`save-file-dialog`、`save-files-dialog`（批量一次弹窗）、`write-generated-output`、`delete-generated-files`、`upload-free-host`、`proxy-request`、`storage-settings`、更新相关等——**均需 main.cjs + preload.cjs + desktop.d.ts 三端同步**
- **自动更新**：启动自动 `checkForUpdates`（来源 `auto`）；**仅启动自动检查发现新版本时弹居中弹窗**（`UpdatePrompt`，含版本号/更新日志/下载按钮/下载进度/重启安装，v0.3.6 起支持暂停/断点续传，见 3.10）；关于页手动检查（来源 `manual`）不弹窗，仅在页内显示状态与日志。来源标记：`checkForUpdate(source)` → `updateState.triggeredBy`（`update-available` 时写入，三端同步）。**CI 发布时自动把 CHANGELOG 段落写入 Release 说明**（`--notes` 从 `$env:GITHUB_WORKSPACE\CHANGELOG.md` 提取）
- **优化提示词**（生图/视频工作台）：TextArea 下方 ModelPicker（capability=text，默认 gpt-5.5）+ Wand2 按钮，`requestImageQuestion` 流式回填
- **参考图**：`ReferenceImageUploader`（生图、视频共用）支持点击/拖拽多选、Ctrl+V、上传中本地缩略图、重试、移除、左右排序、复制 HTTPS 链接、最近上传和手动公网 URL；移除只影响当前任务，不删除 R2 对象。参考视频/音频仍使用本地存储与既有清空按钮。
- **CI 坑**：`git add -u` 不含新文件——新增文件必须显式 `git add`；`publish-desktop.yml` 发布步骤依赖 `GITHUB_WORKSPACE` 绝对路径（勿改回相对路径）

### 3.4 v0.3.2 变更要点

- **渠道启用/关闭开关**（配置页渠道列表最左侧 Switch）：`ModelChannel.enabled?: boolean`（缺省视为启用）。**坑**：persist merge 走 `createModelChannel`（显式对象构造会丢弃未列出的字段），新增渠道字段必须同步加进 `createModelChannel`（`enabled: channel?.enabled !== false`）。禁用生效点：`selectableModelsByCapability`/`selectableImageModelsByFeature` 过滤禁用渠道、`resolveModelChannel` 回退第一个启用渠道、`finishConfig` ready 只统计启用渠道。
- **参考图框内粘贴**：image/video 工作台移除"剪切板"按钮；参考图容器 `tabIndex + onMouseEnter focus + onPaste`，`addReferencesFromPaste` 从 `clipboardData.items` 提取 image/* 文件（无需权限弹窗），无图静默忽略；`addReferences` 参数放宽为 `FileList | readonly File[]`。
- **Agnes 视频对齐官方文档**：高级参数（帧率/种子/推理步数/负面提示词）仅 Agnes 渠道面板显示；请求补发 `frame_rate`/`seed`/`negative_prompt`/`num_inference_steps`/顶层 `mode`；`video_id` 优先查询；`auto` 尺寸不发宽高；帧数按帧率联动（8n+1/≤441）；秒数上限 18、默认横屏 1152x768。

### 3.5 v0.3.3 变更要点

- **移除个人词库**：`PERSONAL_SOURCE_ID`、`personal-prompts.ts`、`personal-prompt-editor-dialog.tsx` 及相关 UI（新增/编辑/删除/一键加入）全部删除，提示词库恢复纯远程来源。
- **资产选择器双按钮**（`asset-picker-modal.tsx`）：卡片去掉整卡点击插入与 hover"插入"覆盖层，底部新增"复制"（text 复制内容 / image 复制 dataUrl / video 复制 url）与"插入"按钮。
- **更新日志显示**（`about-panel.tsx`）：无新版本时 Timeline 显示当前版本（`APP_VERSION`）日志；检查到新版本（`hasNewVersion`）后显示最新版本日志。
- **启动自动检查弹窗**（重新引入 `UpdatePrompt`）：`updateState.triggeredBy` 区分来源——`auto`（启动自动检查）弹居中 Modal（版本号/更新日志/下载/进度/重启安装，同版本只弹一次）；`manual`（关于页手动）不弹窗。

### 3.6 v0.3.4 变更要点

- **音频资产**：`AssetKind` 加 `"audio"`（`AudioAsset`：url/storageKey/bytes/mimeType/durationMs）；素材页支持音频筛选、表单上传（`uploadMediaFile(file, "audio")`）、`<audio>` 预览与下载；画布音频节点可加入资产（`saveNodeAsset` audio 分支，之前误存为图片）；资产选择器音频卡片可复制（url）与插入（画布创建音频节点）；生图/视频/精修页插入音频提示去画布使用。
- **OSS设置 Tab**（v0.3.4 时名为“阿里云设置”，配置对话框「关于」左侧）：`ConfigTabKey` 加 `"oss"`；`OssSettingsPanel` 含 OSS 配置表单（`loadOssHostingConfig`/`saveOssHostingConfig`）与公网 URL"加入参考图"（`enqueueReferenceHandoff({ target: "video", url })` + `dispatchEvent("lyspace:reference-handoff-created")`）；视频页 `consumeReferenceHandoffs` 支持 `handoff.url` 直接加入并监听事件即时刷新；视频页参考图区移除"OSS 设置"/"添加公网 URL"按钮。
- **资产选择器文本内容**：text 资产无封面时封面区直接显示 `data.content`（`line-clamp-5 break-words` 多行排版）。
- **版本更新调整**：关于页移除"最新版本"卡片（仅当前版本）；`use-version-check` 拉取条件改为"本地 `__APP_RELEASES__` 无该版本日志才拉取"（修复更新完成后日志不显示）。
- **Agnes 面板判断修复**：`VideoSettingsPanel` 渠道判断统一基于当前视频模型（`isSeedanceVideoConfig({ ...config, model: videoModel })`），修复原用全局 `config.model` 判断导致选择 Agnes 模型不显示高级参数的问题。

### 3.7 流畅度优化（v0.3.5）

- **画布编辑持久化防抖**：nodes/connections/chatSessions 保存走 500ms trailing debounce（`projectSaveTimerRef` + `pendingProjectSaveRef`），拖拽/生成不再每帧全量 JSON.stringify + IndexedDB 写；切换项目/卸载时 `flushProjectSave` 立即落盘。**坑**：新加画布持久化字段时确认走 pending patch 闭包，勿直接调用 updateProject 高频路径。
- **缩放不重渲染节点**：新建 `useCanvasViewportStore`（`stores/canvas/use-canvas-viewport-store.ts`）保存当前 viewport；`CanvasNode` 移除 scale prop（zoom 不再击穿 React.memo），节点内经 `store.getState().viewport.k` 读取（resize 开始时快照进 resizeRef）。**坑**：该 store 仅供 getState 读取，不要用 selector 订阅（会失去 memo 收益）。
- **rAF 合并**：滚轮缩放（`infinite-canvas.tsx`，基于 viewportRef 累积保证精度）、框选、refine 预览重绘均 rAF 合并，每帧最多一次 setState（连线拖拽 v0.3.8 起改回同步更新，见 3.14）。
- **memo 化**：ConnectionPath（pathD useMemo，onSelect/onContextMenu 必须传稳定引用 `handleSelectConnection`/`handleConnectionContextMenu`）、CanvasTopBar/CanvasToolbar/CanvasZoomControls/CanvasSidePanel（内联箭头回调已提升为稳定 useCallback）、PromptCard（onOpen/onCopy/extraAction 需稳定引用，`prompts/index.tsx` 已 useCallback）。
- **高频写库修复**：video 页 `saveWorkbenchSession` 依赖去掉 elapsedMs（对齐 image 页既有先例，elapsedMs 取闭包最新值）；refine 会话保存 500ms 防抖（`sessionSnapshotRef` 渲染期同步 + 卸载 flush）；refine 调色/LUT Slider 改 onChange 轻量预览、onChangeComplete 才 commitEdits（history 不再每帧 push）。
- **输入框失焦提交**：新建 `useDraftInput`（`hooks/use-draft-input.ts`）+ video 面板输入组件本地 draft 模式，配置弹窗 8 处与设置面板输入不再每键写全局 config store（避免全量 persist 序列化 + 订阅者重渲染），失焦/Enter 提交。**坑**：受控输入必须本地草稿否则输入回跳；外部配置变化（导入）经 value 依赖自动回显。
- **图片加载**：assets 封面 `loading="lazy" decoding="async"`；画布节点 img `decoding="async"`。

### 3.8 缩略图管线与提示词虚拟滚动（v0.3.5）

- **缩略图管线**：新建 `services/thumbnail.ts`——`getThumbnailUrl(source)` 把 blob:/data: URL 原图降采样为最长边 1024 的 JPEG object URL（`createImageBitmap` + canvas + `toBlob`），模块级 LRU 缓存 200 条（淘汰时 revoke），并发上限 3（`runThumbnailTask` 队列），失败返回 null 由调用方回退原图。
  - **画布图片节点**（`canvas-node.tsx` ImageContent）：默认显示缩略图；订阅 `useCanvasViewportStore.viewport.k`（仅本子组件重渲染，CanvasNode memo 外壳不受影响），当 `节点宽 × 缩放 > 1024×1.2` 时切回原图避免放大模糊。**坑**：缩略图 URL 是共享缓存，组件切换时不可 revoke（LRU 淘汰才 revoke）；图片内容变化（新 blob URL）自动走新 key，旧条目由 LRU 回收。
  - **assets 封面**（`pages/assets/index.tsx` AssetCard）：同样走缩略图，生成失败回退原图。
- **提示词虚拟滚动**（`pages/prompts/index.tsx`）：引入 `@tanstack/react-virtual`，按窗口断点（1280px→3 列 / 640px→2 列 / 其余 1 列）把卡片分组成行，行虚拟化 + 动态测量（`measureElement`），overscan 4，滚动容器为原 `<main>`（`scrollRef`）；无限滚动加载（`handleListScroll` 滚到底 fetchNextPage）保留不变；`PromptGrid` 组件已删除。**坑**：行高由封面（aspect 4:3 固定）+ 内容决定，首次渲染即稳定，动态测量一次后不再漂移；resize 跨断点会重新分组，滚动位置可能有轻微偏移（可接受）。

### 3.9 v0.3.5 节点悬浮工具条自动换行

- **工具条换行**（`canvas-node-hover-toolbar.tsx`）：容器去掉固定 `h-12`，改 `flex flex-wrap justify-center gap-y-1` + `max-w-[calc(100vw-32px)]`——图片节点全工具（原约 1300px 单行）超过视口宽度自动折为两行；短工具条（文本节点）仍单行。**坑**：absolute flex 容器不加 `max-w` 时宽度收缩到内容、`flex-wrap` 不生效；按钮内部 `whitespace-nowrap` 保持不折行，换行是工具条级。

### 3.10 v0.3.6 更新下载断点续传与弹窗改造

- **自研断点续传下载器**（`desktop/main.cjs`）：保留 electron-updater 的版本检查（`update-available` 事件缓存 `files[0]` 的 url/sha512/size），下载改为 `net.request` + `Range: bytes=N-` 续传写 `userData/updates/<文件名>.part`，暂停保留 `.part`（`pauseUpdateDownload` → abort + flush，状态 `paused`），继续调用 `downloadUpdate()` 自动从断点续传；完成后 sha512（base64 去 padding）校验，通过才改名为 `.exe` 进入 `downloaded`，校验失败删 `.part` 报错。
- **安装方式变更**：`requestUpdateInstall` 不再用 `autoUpdater.quitAndInstall`，改为 `persistence-flushed` 后启动 assisted NSIS 安装器并 `app.quit()`；当前只传 `/D=${path.dirname(app.getPath("exe"))}`（必须作为最后一个整体参数），不传 `/S` 或 `--force-run`，也不隐藏窗口，因此用户可见安装向导并在完成页重新运行应用。下载文件 URL 拼 `https://github.com/LightyearXizIl/LY-Space/releases/download/<tag>/<files[0].url>`（tag 带 v，文件名不带 v）。
- **三端同步**：IPC `lyspace:cancel-update-download` → `lyspace:pause-update-download`（`pauseUpdateDownload`）；`AppUpdateStatus` 新增 `"paused"`（desktop.d.ts + 主进程状态机）。
- **弹窗**（`update-prompt.tsx`）：`styles.wrapper` flex 强制正中心（替代仅 `centered`）；`maskClosable={false}` `keyboard={false}`；`open` 覆盖 available/downloading/paused/downloaded/error（下载失败、暂停均驻留，仅「稍后」关闭）；下载中「暂停」、暂停后「继续下载」+「稍后」、失败显示错误 +「重试」。
- **关于页**（`about-panel.tsx`）：下载中「取消下载」→「暂停」；`paused` 显示「已暂停 x / y」+「继续下载」；检查按钮 disabled 适配 paused。

### 3.11 v0.3.6 关闭最小化到系统托盘

- **关闭行为**（`desktop/main.cjs`）：`mainWindow.on("close")` 中 `allowWindowClose` 为 false 时 `preventDefault()` + `mainWindow.hide()`——点右上角 X 只隐藏到系统托盘，不退出；数据由现有防抖/异步落盘持续保存（隐藏不触发 flush）。
- **系统托盘**：`createTray()`（whenReady 末尾调用）：图标 `build/icon.png` resize 32×32，tooltip「LY Space」，**左键单击**显示主界面（show/restore/focus），**右键菜单**：显示主界面 / 关闭。
- **托盘「关闭」= flush 后退出**：菜单「关闭」发送 `lyspace:flush-persistence` → 渲染进程落盘 → `persistence-flushed` → `allowWindowClose=true` → `mainWindow.close()` 真退出（复用既有流程，数据安全）。
- **second-instance**：`!mainWindow.isVisible()` 时先 `show()` 再 focus（窗口隐藏时再次启动实例恢复显示）。
- **坑**：① `tray` 必须是模块级引用（局部变量会被 GC 导致图标消失）；② 隐藏 ≠ 退出，不要再在 close 里发 flush（托盘关闭才走 flush 流程）；③ `closingTimer` 10 秒等待警告已随旧 close 流程移除。

### 3.12 v0.3.7 新建渠道草稿化 + 默认渠道 OpenAI

- **新建渠道草稿化**（`app-config-modal.tsx`）：点「新增渠道」只创建临时草稿（`editingDraft` state + `editingChannelId`）打开编辑抽屉，**不写入 channels**——不点保存直接关闭即丢弃，重启后无残留；`saveChannel` 按 id 是否已存在区分「新增（push）/编辑（替换）」，保存成功才持久化并清草稿。
- **默认渠道 OpenAI**（`use-config-store.ts` `defaultConfig`）：baseUrl/apiFormat 改 `OPENAI_BASE_URL`/`"openai"`，`channels[0]` 改为 OpenAI 渠道（纯 OpenAI 模型：gpt-image-2/gpt-image-2-vip/nano-banana-2/nano-banana-fast/gpt-5.5/gpt-5.4，去掉 gemini）。**不包含任何 key**：全部 apiKey 留空，用户自行填写（全代码无硬编码密钥）。
- **坑**：① `defaultConfig` 仅影响新安装/重置配置的用户，已装用户持久化配置不受影响；② `GRSAI_DEFAULT_MODELS` 保留（`services/api/image.ts:1167` grsai 格式模型列表仍依赖）；③ `default::gpt-image-2`/`default::gpt-5.5` 默认模型引用依赖 default 渠道含这两个模型名，改模型列表时别删。

### 3.13 v0.3.7 画布左键框选

- **左键空白处 = 框选**（`infinite-canvas.tsx` `handlePointerDown` + `project.tsx` `handleCanvasMouseDown`）：普通左键按住空白拖动出现框选矩形并选中相交节点；点击空白（无拖动）= 取消选择；Shift+左键 = 追加框选；Ctrl/⌘+左键同样框选（兼容旧习惯）。
- **平移改为中键 或 空格+左键**：原「左键空白=平移」让位给框选；原「空格+左键只 preventDefault 的占位分支」顺带修复为真正平移。
- **坑**：① 节点/连线上的左键行为不受影响（`isBackgroundClick` 隔离）；② `handleCanvasMouseDown` 的 ctrl/meta 限制已移除——若以后要恢复「Ctrl 才框选」需同时改两处；③ 框选路径复用 `selectionBoxRef` + rAF 合并（性能优化后每帧最多一次相交计算）。

### 3.14 v0.3.8 画布指针/选择/连线优化 + 标题栏主题 + 数据保留保障

- **画布空闲指针为系统默认**（`infinite-canvas.tsx`）：容器去掉 `cursor-grab`，空闲时系统默认箭头；平移中仍 `grabbing`（body style）。
- **节点编辑框/上方工具栏仅选中时显示**（`project.tsx`）：`handleCanvasMouseDown` 启动框选时补 `setDialogNodeId(null)` + `setToolbarNodeId(null)`（此前框选空白只清选择，面板/工具栏残留）；`deselectCanvas` 与 `keepNodeToolbar`（要求已选中）逻辑不变。
- **连线拖拽改回同步更新**（`project.tsx`）：移除 `connectionDragRef`/`connectionFrameRef` rAF 合并，mousemove 直接 `setMouseWorld` + `setConnectionTargetNodeId`（连线是单条低频交互，同步最跟手；框选的 rAF 合并保留）。`connectNodes` 补反馈：连自己提示「不能连接到自身」、重复连接提示「该连接已存在」。
- **窗口标题栏跟随应用主题**：主进程 `nativeTheme.themeSource`（IPC `lyspace:set-native-theme`，preload `setNativeTheme`）；`AppProviders` 主题变化时同步调用——Windows 标题栏/原生控件明暗随应用内浅/暗切换。
- **关于页更新区**：核对 stone + `dark:` 变体已全适配，无硬编码颜色。
- **当时的数据保护结论已被 v0.4.7 推翻**：旧 `customRemoveFiles` 的 `CopyFiles *.*` 实际不递归，无法保护安装目录里的 IndexedDB/Local Storage/Result 子目录；以 3.23 的双快照迁移方案为准。升级前 `flush-persistence` 仍保留。

### 3.15 v0.3.9 镜头模块 + 画布节点提示词优化

- **镜头模块**（`components/camera-module.tsx`，新增）：5 组单选按钮（视角：自动/正面/45度/侧面/背面/俯视/仰视；景别：自动/特写/半身-中景/全身/产品细节/场景/远景；距离：自动/近/中/远；镜头：自动/广角/标准/长焦/微距；机位：自动/平视/高机位/低机位），默认"自动"= 不产生文本。点击选项把文字以"，"追加到提示词末尾，再点已插入项移除，组内切换先删旧插新，点"自动"清空该组。**选中态由提示词文本派生**（`cameraTextPresent` 独立词匹配），外部改文本（优化回填/手改/切节点）高亮自动同步，无需 state。
- **文本操作纯函数**：`appendCameraText`/`removeCameraText`/`cameraTextPresent`（导出可测）。独立词正则 `(^|分隔符)词(?=$|分隔符)`——避免单字选项误伤长词（"中"不误配"半身/中景"、"远"不误配"远景"）；remove 删最后出现、开头残留分隔符自动清理。**坑**：新增镜头选项时注意与既有选项/用户词的字串包含关系。
- **接入点**：① 生图页/视频页提示词区块正下方（`<CameraModule value={prompt} onChange={setPrompt} />`）；② 画布生成节点面板 `canvas-node-prompt-panel.tsx`（仅 image/video 节点显示，`theme` 适配画布明暗、`showTitle={false}` 紧凑）；③ 生成配置节点 `CanvasConfigComposer`（编辑器下方，`value/onChange` 受控，点击后失焦触发 useEffect tokens 重建显示）。
- **画布提示词优化**（`hooks/use-prompt-optimizer.ts`，新增）：与生图/视频页 optimizePrompt 同源逻辑（textModel 优先、`requestImageQuestion` 流式回填、isAiConfigReady 校验+openConfigDialog 引导），供画布面板与 Composer 复用。画布面板 `updatePrompt` 语义沿用：`isEditingExistingContent` 时优化结果只更新本地 prompt，提交才写回 metadata。
- **Composer 优化保留 @ 引用**：system prompt 明确要求原样保留 `@[node:xxx]` 令牌（hook 支持 `systemPrompt` 自定义参数），回填后 `parseComposerTokens` 自动还原引用 chips；模型删改令牌会导致对应引用丢失（可接受，用户可重新 @ 引用）。
- **坑**：① 提示词优化整体覆盖输入框文本（含已插入镜头文字）——与生图/视频页行为一致，镜头按钮高亮自动随文本消失；② Composer 编辑器有焦点时 value 变化跳过 DOM 重建（既有机制），点击镜头/优化按钮会先失焦再重建，文本正常显示。
- **坑（浅色模式 Alert 深灰）**：antd 主题 token 的 `colorInfo` 曾跟随 `color.primary`（浅色 #171717 黑）→ `colorInfoBg` 由黑色板派生为 #575757 深灰，导致 Alert info（关于页"发现新版本"等）在浅色模式下是深灰块；暗色模式 primary=#fafafa 白还会派生白底白 icon。已移除 `colorInfo` 覆盖恢复 antd 默认 #1677ff。**教训：`colorInfo`/`colorInfoBg` 等派生 token 不要设为黑/白等极端色，除非显式同步设置对应 Bg/Border**。

### 3.16 v0.4.0 镜头模块重构 + 画布交互改造

- **镜头模块 UI 重构**：① 画布节点面板：`CanvasCameraPopover`（新组件，仿 `CanvasImageSettingsPopover` 的 portal fixed 定位 + 外部点击关闭）按钮放在工具栏行**图像/视频设置右侧**，点击展开；② 生图/视频页：`CameraTrigger`（新组件，antd Popover）放在**设置区"模型"下拉右侧**（GenerationSettings 模型行改 flex 一行两列，移动端 Drawer 复用自动生效）；③ Composer 保留内联展开。页面场景选中态改为柔和样式（浅色 `border-stone-900 bg-stone-100 text-stone-900`、暗色 `border-stone-100 bg-stone-800 text-stone-100`），不再整块反色。
- **画布优化模型选择**：`CanvasNodeMetadata.textModel`（提示词优化专用文本模型）；两处 `buildNodeConfig` 透传 `textModel: metadata.textModel || globalConfig.textModel || defaultConfig.textModel`；面板优化行（image/video 节点）与 Composer 优化行均加 `ModelPicker(capability="text")`（Composer 经新增 `onConfigChange` prop 持久化到 Config 节点 metadata）。
- **右键节点 = 工具栏**：不再"选中即显示"（`selectNodeByEvent` 移除 setToolbarNodeId）；右键节点在**点击处**弹出工具栏（`toolbarPosition` fixed 坐标，视口 clamp），`flex-col flex-wrap max-h-[340px]` **竖向两列**（文字横写），未选中节点右键会先单选选中；节点右键菜单（复制/删除）移除——`CanvasNodeContextMenu` 仅剩连接线删除；复制入口改为快捷键/Alt 拖拽。工具栏清理链路（空白 mousedown/Esc/删除/图片面板）同步清 `toolbarPosition`。
- **空格立即抓手**：keydown 空格即 `body.cursor="grab"`（不再等左键点按），配合左键才平移；`spacePressedRef` 同步供 pointerdown 判断（避免同帧 state 滞后）；keyup 恢复默认（平移中不抢）；**排除 INPUT/TEXTAREA/contentEditable**（Composer 输入空格不触发）；pointerup 松左键时空格仍按住回 grab。
- **空白右键 = 添加节点**：`preventCanvasContextMenu` 改为打开 `NodeCreateMenu`（screenToCanvas 定位）；双击空白创建已移除（infinite-canvas 删除 onDoubleClick 接线）。
- **Alt+左键拖拽复制**（含框选组）：`handleNodeMouseDown` 检查 `event.altKey` → 原位克隆 dragIds（新 id、title 加 Copy、`groupId`/`batchChildIds` 重映射）并复制两端都在克隆集的连线（同 Ctrl+C/V 语义）→ 拖拽副本。**坑**：① 克隆 setState 与 `historyPausedRef=true` 同帧批处理，渲染时 paused 已生效 → 克隆不进历史，**Alt 点击未拖动时必须撤销克隆**（`altCloneIdsRef` 记录 + finishNodeDrag 回滚，`hadMoved` 快照须在 dragRef 重置前保存）；② 撤销分支同步 `lastHistoryRef.current` 引用，使 history effect 引用比较短路，避免空历史条目；③ 拖动位移在 paused=false 后提交 → undo 一步回滚克隆+位移。
- **图片预览滚轮缩放**：新组件 `CanvasImagePreviewModal`（原内联 Modal 替换），滚轮直接缩放 0.2x–5x（±10%/步），底部悬浮条显示比例 + 重置 + 放大按钮，打开/切换节点重置。
- **资产侧栏**：`ASSET_GROUPS` 加音频分组；"添加"按钮改 Dropdown：本地上传（accept 加 `audio/*`，handleFiles 加 audio 分支）/ **从画布添加**（Modal 列出有内容的图片/视频/音频/文本节点，调 `saveNodeAsset`，经 `onSaveNodeAsset` prop 从 project.tsx 传入）/ **新增文本**（Modal TextArea → `addAsset({kind:"text"})`，标题取前 24 字）。
- **快捷键提示更新**（canvas-zoom-controls + canvas-top-bar 两处）：空格/中键平移（空格即抓手）、Alt+左键拖动复制、右键节点=工具栏、空白处右键=添加节点、双击创建条目移除。
- **v0.4.1 修正**：① `toolbarNode` 移除 `|| singleSelectedNodeId` 单选回退（v0.4.0 只改触发点漏删此处，选中节点仍会显示工具栏）——现在仅右键节点触发；② 镜头弹层组行改"标签固定列（w-8）+ 按钮独立 flex-wrap 换行区"，换行不再越过标题列；③ 资产侧栏搜索框下新增类型筛选（全部/图片/视频/文本/音频，`typeFilter` state，与标签筛选/搜索共存）；④ 生图/视频页设置区模型行改 `grid-cols-2` 左右分列（左「模型」+下拉、右「镜头」标题+按钮，两标题同高，移动端 Drawer 复用）；⑤ 图片预览 Modal 放大后**可按住拖动平移**（scale>1 生效：pointer capture + dragOffset，`translate+scale` 变换，`dragging` state 控制 transition 与光标，按钮区域不参与拖动，重置同时清位置；打开/切节点重置）。


### 3.17 DeepSeek_Visualmodel 看图技能（Reasonix 环境，非应用内功能）

- **用途**：会话主模型（DeepSeek V4 Flash）无原生视觉，用户发图时通过 `eyes.py` 路由到外部视觉模型转成文字描述，再基于描述回答
- **技能位置**：`E:\Vibe coding\skills\DeepSeek_Visualmodel\`（**共享技能目录**，已注册进全局 `[skills] paths`，所有项目可用；SKILL.md + eyes.py + README.md，技能名 `DeepSeek_Visualmodel`）。**源项目 `E:\Vibe coding\deepseek-eyes-master` 已不存在（被清理）**，代码唯一来源即 `E:\Vibe coding\skills`
- **调用方式**：`python "E:\Vibe coding\skills\DeepSeek_Visualmodel\eyes.py" "<图片路径>" --prompt "<用户问题>" [--provider zhipu|gemini|siliconflow|dashscope]`
- **GitHub 仓库**：`https://github.com/LightyearXizIl/skills_Collection`（公开，main 分支，本地根 `E:\Vibe coding\skills`，初始 commit 4e99902；git 身份 LightyearXizIl / LightyearXizIl@users.noreply.github.com（仓库级）；gh CLI 未登录，用 Windows 凭据管理器 git 凭据推送）
- **合集规范**：每个技能一个子目录（SKILL.md frontmatter 必须 name+description）+ 目录内 README.md（用途/用法/key 配置）+ **禁止任何 API key**（一律环境变量）；新增技能后需显式 `git add` → commit → push
- **4 个 provider（均实测通过）**：zhipu（默认，`glm-4.6v-flash` 免费，128K）/ gemini（`gemini-3.6-flash` 免费层，用户网络可直连 Google）/ siliconflow（`PaddlePaddle/PaddleOCR-VL-1.5` 免费 OCR）/ dashscope（百炼，key 额度已耗尽 403，勿用）
- **环境变量**（均写入 Windows 用户级）：`ZHIPU_API_KEY`、`GEMINI_API_KEY`、`SILICONFLOW_API_KEY`、`DASHSCOPE_API_KEY`
- **坑**：① 智谱旧格式 key 是 `id.secret`，必须签 HS256 JWT（`zhipu_bearer_token` 已内置处理），`sk-` 开头新格式直传 Bearer；② SiliconFlow 的 OCR 模型必须用真实尺寸图片（1x1 会 400）；③ 免费模型偶发 429 限流 / SSL EOF 网络抖动，稍等重试即可；④ Gemini 免费层有限速，日常默认 zhipu，精细看图切 `--provider gemini`、提取文字切 `--provider siliconflow`

### 3.18 v0.4.2 变更要点

- **资产侧栏类型筛选标签**（`canvas-side-panel.tsx`）：选中态由黑底白字改为柔和样式（浅色 `bg-stone-100 text-stone-900`、暗色 `bg-stone-800 text-stone-100`，与页面场景选中态一致）；类型标签容器改 `flex-nowrap` + `overflow-x-auto`，5 个类型标签一行不折行（侧栏极窄时横向滚动兜底）。**坑**：下方 allTags 标签筛选行仍保持 `flex-wrap`（标签数量多，勿一并改死）。
- **生图/视频页模型行与镜头按钮**（`image/index.tsx`、`video/index.tsx`、`camera-trigger.tsx`）：模型行由 `grid-cols-2` 对半改为 `grid-cols-[minmax(0,1fr)_auto]`——模型下拉占满剩余宽度（长模型名不再截断），镜头按钮紧凑靠右；镜头按钮由 antd Button 改为与 `SelectTrigger` 同款胶囊样式（`h-8 rounded-full border bg-transparent px-3 shadow-sm`），移除 `!w-full`。**坑**：`CameraTrigger` 的 `placement` 仍默认 `bottomRight`（弹层以按钮右缘对齐，本次未改），如需按钮正下方弹出需改 placement。

### 3.19 v0.4.3 变更要点

- **镜头模块移入设置区右列**（`image/index.tsx`、`video/index.tsx` GenerationSettings）：桌面端外层为 `sm:grid-cols-2` 两列，此前整个 GenerationSettings 占左列（内部再分模型/镜头），**右列整行空白**（用户截图黄框位置）。本次把「模型」「镜头」拆为外层 grid 的两个独立列——模型占左列、镜头整体移入右列（与模型同排、标题同高），`ImageSettingsPanel` 仍跨两列；移动端 Drawer（外层同为 2 列）模型/镜头继续左右并排。**坑**：GenerationSettings 的 fragment 子元素直接参与外层 grid 排布，模型/镜头 div 不得再设 `col-span-2`（否则又回到单列内部排布）；v0.4.2 的 `grid-cols-[minmax(0,1fr)_auto]` 内部分列实现已被此方案取代。

### 3.20 v0.4.4 变更要点

- **画布图片黑块修复（关闭/重开项目有概率出现）**：根因是**共享 storageKey 被误删**——生图工作台记录「添加到资产」时资产与记录共享同一 `storageKey`（`pages/image/index.tsx` saveResultToAssets），资产插入画布后画布节点也引用它；删除工作台记录时 `deleteStoredImages` **不检查其他引用**，直接 revoke blob URL + 删 IndexedDB blob → 当前会话图片已解码仍显示，但关闭/重开项目重新水合时 `resolveImageUrl` 找不到 blob 返回失效 URL → 节点透明背景透出画布底色 = 黑块。
  - **修复 A**（`pages/image/index.tsx` `deleteSelectedImages`）：删除前用 `collectImageStorageKeys({ assets, projects })` 收集仍被资产/画布引用的 key，仍被引用的 blob 保留（记录本身照常删除）。
  - **修复 B**（`components/canvas/canvas-node.tsx` ImageContent）：图片加载失败自动降级——缩略图失败回退原图 → 原图失败按 `storageKey` 调 `resolveImageUrl` 从 IndexedDB 重新解析 → 仍失败显示「图片加载失败」占位（不再黑块）。`onError` 触发后 setState 换 src，无循环。
  - **坑**：① 所有「结果图 → 资产/参考图 → 画布」共享 storageKey 的链路，删除任一端都要先查其他引用；② 旧版本已把 blob 删掉的黑块节点数据无法找回，只能显示占位；③ 视频/音频节点（`file-storage.ts` 的 media key）理论上存在同样的共享删除风险，当前未处理（无对应删除入口触发）。
- **画布库项目拖动排序**（`pages/canvas/index.tsx` + `stores/canvas/use-canvas-store.ts`）：卡片可拖拽自定义排序，`reorderProjects(id, targetId, before)` 移动项目并持久化（projects 数组顺序即展示顺序）；网格内按卡片对角线判断插入前/后（`(clientY-top)/height < (clientX-left)/width` → 之前），换行到上下行也正确；被拖卡片半透明、目标卡片边缘显示蓝色插入指示线（`inset 3px` boxShadow）；从按钮/输入框上不触发拖动（`closest("button, input, a")` 拦截）。**坑**：新建项目仍 push 到数组最前（用户可再拖走）；排序状态是本地 state，`dragend`/`drop` 都要清理。

### 3.21 v0.4.5 变更要点

- **画布工具栏「编辑/取消生成」**（`canvas-node-hover-toolbar.tsx` + `project.tsx`）：① `canOpenDialog` 由 `hasImage` 改为 `isImage`——生成中/空内容图片节点也有「编辑」入口（原条件 `isText || hasImage || isVideo`，加载中节点没有 content 导致面板打不开）；② 新增「取消生成」状态工具：`status === "loading"` 时显示，`onCancelGenerate` 直接调 `stopGenerationByRunningId(node.id)`（无确认弹窗，单节点/批量子节点 runningNodeId 都是节点自身 id）；③ 取消生成不参与图片快捷工具开关过滤（`hasImage && loading` 的重新生成场景也要显示），`selectableImageToolbarTools` 同步排除。
- **图片详情弹窗任意缩放可拖动**（`canvas-image-preview-modal.tsx`）：移除 `scale <= 1` 时禁止拖动的限制，任意缩放级别（20%–500%）都可按住拖动平移；光标恒为 grab/grabbing。**坑**：滚轮缩放 ±10%/步、重置同时清位置、按钮区不参与拖动的既有行为保持不变。
- **组装提示词编辑框（Composer）镜头弹层化**（`canvas-config-composer.tsx`）：移除内联平铺的 `<CameraModule>`（5 行 27 按钮），复用 `CanvasCameraPopover` 按钮+弹层，放在底部操作行左侧（模型选择器旁），与画布节点面板/工作台样式统一。**坑**：Composer 编辑器有焦点时 value 变化跳过 DOM 重建的既有机制不受影响（镜头按钮点击会先失焦）。
- **视频/图片节点编辑面板拆两行**（`canvas-node-prompt-panel.tsx`）：图片/视频节点的提示词工具行（提示词库 + 优化文本模型 + 优化按钮）改为**左对齐**放在提示词框下方，生成工具栏（模型/设置/镜头/生成按钮）独立一行——此前 gpt-5.5 行右对齐悬在「镜头/发送」上方形成双层冲突；音频/文本节点保持原布局（提示词库仍在生成工具栏左侧）。
- **生图/视频工作台「模型/镜头」行**（`pages/image/index.tsx`、`pages/video/index.tsx`）：设置区外层 grid 由 `sm:grid-cols-2` 改为 `sm:grid-cols-[minmax(0,1fr)_auto]`（移动端 Drawer 同样），模型下拉占满剩余宽度不再截断，镜头按钮紧凑靠右。**坑**：v0.4.3 的 50/50 两列布局被此方案取代；`ImageSettingsPanel` 的 `col-span-2` 仍跨两列不受影响。
- **画布复制/克隆只复制选中节点**（`project.tsx` Alt 克隆 + `pasteCopiedNodes`）：① 批量组根节点被选中时其 `batchChildIds` 子图不再随 Alt 克隆（`selectedBatchRootIds` 过滤），副本根节点 `batchChildIds` 清空（显示为单图）；② **输入连线同步**：源节点不在复制集合时，复制一条连线指向原源头节点（`fromNodeId` 保留原 id、`toNodeId` 映射新 id）——「只要线不要源头节点」，Alt 克隆与 Ctrl+C/V 粘贴行为一致；③ Alt 单击未拖动撤销逻辑（`altCloneIdsRef`）已覆盖新增输入连线。**坑**：拖拽（非 Alt）移动组时 `dragIds` 仍含批量子图（不变）；两端都在复制集合的连线仍按原逻辑重映射。
- **组节点固定（钉子）**（`types/canvas.ts` + `canvas-node.tsx` + `project.tsx`）：`CanvasNodeMetadata.pinned?: boolean`；组节点右上角（「N 个节点」标签旁）新增 Pin 按钮，点击固定/取消；`isNodeLocked` 动态判定——组 pinned 时组自身及所有 `groupId` 指向该组的成员（含后续拖入/新建）不可拖动；`handleNodeMouseDown` 计算 `dragIds` 后若有任一锁定节点直接 return（不建 dragRef、不触发 Alt 克隆）。**坑**：① 锁定只拦移动，选中/删除/调整组帧大小/连线仍可用；② 框选拖拽含锁定节点时整组拖动被拦（安全优先）；③ pin 状态随节点持久化，重开项目保留。

### 3.24 v0.4.8 变更要点

- **事故根因**：v0.4.7 在 `app.requestSingleInstanceLock()` 失败后只调用 `app.quit()`，但当前同步调用栈仍继续执行 `configureStorageBeforeReady()`；两个进程可能同时把固定临时目录 `Data cache.migrating-v0.4.7` 原子切换到同一个非空目标，触发 `ENOTEMPTY`。恢复异常随后被重新抛出，又产生第二个 Electron 主进程错误框。
- **迁移竞态修复**（`storage-migration.cjs` + `main.cjs`）：没有取得 Electron 单实例锁的进程不再执行 `configureStorageBeforeReady`；迁移增加 PID 排他锁，目标已是完整快照时直接补完成标记，原子切换遇到并发空目录时安全重试，不同内容继续独立备份后再替换。v0.4.7 遗留的 `Data cache.migrating-v0.4.7*` 不自动删除，用户可直接覆盖安装重试。
- **安装回执状态机**（`update-install-coordinator.cjs`）：每次 flush 带唯一 requestId，15 秒超时会撤销请求；迟到或重复回执只忽略，不得关闭应用或启动安装器。界面新增 `installing` 状态和可读错误，本地写 `app-data/update-install.log`。
- **静默安装重开**：assisted NSIS 更新参数改为 `/S --force-run /D=...`（`/D=` 仍必须最后），安装完成后自动启动新版本。启动恢复失败只显示一次数据保护错误，不再继续抛出 Electron 主进程异常框。
- **验证结果**：Node 数据迁移/更新状态机 12 项、渲染层 GRSAI 12 项，共 24 项测试通过；类型检查、主进程语法检查、版本一致性、渲染构建、NSIS x64 打包与包元数据校验通过。独立应用名/appId/userData/安装目录中实际构造 `status: failed`、非空目标和遗留迁移目录，恢复变为 `completed`，画布/设置哨兵逐文件一致，旧迁移目录及备份仍存在；静默覆盖安装退出码 0，安装后主进程自动重开。
- **发布物**：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.4.8`；远端资产为 `LY-Space-Setup-0.4.8.exe`、对应 blockmap、`latest.yml`，其中 `latest.yml` 的 version/path/size/sha512 已核对。正式本地安装包为 `E:\Vibe coding\LY Space\LY Space-源码\release\LY Space Setup 0.4.8.exe`，本地 SHA-256：`F74AE2E4912A32C9B6AF94A264542432500BEABAE8EE1BA0BC03A329F7FD7311`。
- **当前用户侧恢复边界**：截图设备账户为 `EDY`，备份根为 `C:\Users\EDY\AppData\Local\LY Space\Backups\v0.4.7-20260815-105710-452a3778`，安装目录为 `D:\Software\Professional software\LY Space`。该设备尚未回传 v0.4.8 覆盖安装后的最终数据验收结果；接手者不得把“隔离环境通过”误写为“EDY 真实数据已恢复”。若仍失败，只收集 `app-data\migration-v0.4.7.json` 与错误文字，禁止让用户删除或上传可能含 API Key 的数据目录。

### 3.25 v0.4.9 镜头参数与画布修复

- **镜头参数隐藏发送**：视角、景别、距离、镜头和机位改为独立结构保存，不再把镜头文字写入用户提示词；发起图片/视频请求时才组装描述，并在工作台记录、画布节点与重试链路中回显复用。
- 批量根节点（`metadata.isBatchRoot`）失败后显示「一键重试」，仅并发重试失败子项；已成功图片不会重跑。重试期间根节点为生成中，首张成功图片会回写为根节点预览；全部结束后按剩余失败项更新状态，并保留第一个真实错误。
- 新建手动连线会记录可选 `fromSide` / `toSide`（`left | right`）。松手时优先使用实际命中的 DOM 节点与端口；命中节点主体时取距鼠标最近的侧边，几何容差只作兜底。旧连接和插件创建连接没有侧边字段时仍按「右出左入」绘制，无需迁移，不影响导入导出、复制粘贴、撤销重做或 `connect_nodes`。
- 图片、视频、音频与文本请求统一优先提取上游 `error`、`message`、`msg`、`detail`、`data` 中的原始错误正文，不再用「生成失败」或渠道前缀覆盖；仅当上游没有正文时才使用 HTTP 状态或本地兜底。错误区域支持完整换行和滚动查看。
- 批次子节点只在仍保持「根节点→子节点」连接时随根节点级联删除；连接线已删除的子节点会保留并清除失效的 `batchRootId`。
- **验证与发布**：发布前已执行定向 Vitest `tests/grsai-image.test.ts`（12 项）与 `tests/canvas-retry-connection.test.ts`（4 项）；新增的 `tests/canvas-node-deletion.test.ts` 未执行，人工画布验收仍在待测文档。首次 CI 因可选 `metadata` 的 TypeScript 收窄失败，提交 `9e226d0` 修复后，第二次 CI 的版本校验、类型检查、渲染构建、Windows NSIS 打包与包验证均通过。
- **发布物**：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.4.9`；远程资产为 `LY-Space-Setup-0.4.9.exe`（92532493 字节）、对应 blockmap 与 `latest.yml`。`latest.yml` 的 version/path/size/sha512 已与 EXE 核对；本地未另行打包。

### 3.26 v0.5.0 更新可见安装与存储路径保护

- **存储路径是权威配置**：新增 `desktop/storage-settings.cjs`。有效 `storage-settings.json` 中的 `resultRoot`、`cacheRoot` 会原样保留；仅配置不存在或字段缺失时补默认值。配置损坏、无效或目录不可写时主进程停止启动，绝不写回空白默认路径。保存时保留未知配置字段。
- **一次性迁移**：`storage-migration.cjs` 仅在旧安装目录默认数据完成快照、逐文件校验与原子恢复后，才把相应配置字段改为稳定默认路径；外部自定义路径及安装目录内自定义子目录保持原值。
- **可见更新安装**：更新器去除 `/S`、`--force-run` 与 `windowsHide`，继续等待持久化回执后启动可见的 NSIS 向导，预填当前程序安装目录；完成页默认提供重新运行。弹窗状态改为「正在保存并打开安装程序...」。
- **版本与验证**：版本号三段的每一位只允许 `0` 到 `9`；补丁位到 `10` 时进位为中间位（`0.4.9` → `0.5.0`），中间位到 `10` 时进位为主版本。Node 数据保护/安装状态机共 19 项通过，版本一致性通过；`Publish LY Space Desktop` 运行 `31873942364` 成功，正式 Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.0` 包含 `LY-Space-Setup-0.5.0.exe`（92533487 字节）、对应 blockmap 与 `latest.yml`。`latest.yml` 的 version/path/size 与远端 EXE 一致且含 SHA-512；GitHub API 提供的 EXE SHA-256 为 `426b09aa359afaaa9d19117ff0d33c91e762f7dbfe9eacdbfc44c9d34675e441`。误发的 `v0.4.10` Release 与 tag 已删除。真实用户数据升级与可见安装器仍需在隔离环境人工验收。

### 3.27 旧版本（≤0.4.6）直接升级 0.5.0 的坑（2026-08-18 修复）

- **升级前必须完全退出 LY Space（含系统托盘）**：`backup-user-data.ps1` 只等 90 秒，进程仍在就直接中止安装（弹「用户数据备份或校验失败，升级已停止」）。0.3.6+ 点窗口 X 只是隐藏到托盘，用户以为关了其实进程还在——这是 0.4.6 用户升级失败的第一大原因。0.4.6 应用内更新 spawn 安装器后立即 `app.quit()`，退出不干净同样触发。
- **0.4.6 自动更新调 0.5.0 安装器仍传 `/S`（静默）**：备份失败时 MessageBox 被 `/SD IDOK` 自动关闭、安装直接 Abort，用户看不到任何报错。诊断日志在 `%LOCALAPPDATA%\LY Space\Backups\upgrade-backup-error.log`（本次新增 trap 写入）。
- **ps1 必须带 UTF-8 BOM**：PowerShell 5.1 对无 BOM 文件按 ANSI 解码，中文错误信息会乱码（显示为「LY Space 源氤渣澹…」），且部分中文字符（如「右」）的字节序列会触发解析语法错误导致安装器误报。2026-08-18 起 `backup-user-data.ps1` 带 BOM；改动此文件后必须保留 BOM 并跑 `desktop/tests/storage-migration.test.cjs`。
- **0.4.6 卸载器会破坏安装目录数据**（`CopyFiles` 不递归 + `RMDir /r`，嵌套子目录丢失）；升级安全完全依赖安装器先做的 SHA-256 快照备份，备份成功前任何环节中止都不删数据。
- **「无法写入 …\Temp\nse*.tmp\app-64.7z」**：NSIS 解压 payload 失败，多为杀软拦截或 Temp 空间不足，与版本兼容性无关；建议临时关闭安全软件或清理 Temp 后重试。

### 3.28 v0.5.2 画布操作与运行日志

- **画布生成取消**：生成请求按结果节点登记独立 `AbortController`；右键「取消生成」仅中断所点节点，批量同组其他节点继续。源节点编辑面板的「停止」仍取消整批；所有节点取消后源节点和汇总节点恢复空闲，不显示失败。
- **复制原图**：已完成图片节点右键工具栏固定提供「复制图片」，优先读取 `storageKey` 的原图 Blob，必要时才按内容 URL 回退；Electron 主进程以原生图片写入 Windows 剪贴板，WebP 会转为等尺寸 PNG，不使用 1024px 缩略图。
- **画布库排序**：拖动目标卡片按水平中线判定前后插入，提示线和松手重排使用同一函数；松手时直接重算，避免 React 状态延迟导致最终位置与指示线不一致。
- **运行日志**：设置新增「运行日志」，记录系统、网络/API、错误和关键操作，支持筛选、复制、导出、打开 `app-data/app.log` 目录与清空。主进程最多保留约 4 MiB，超限后保留最新约 2 MiB；API Key、密码、令牌、鉴权信息和请求内容会脱敏或不记录。
- **发布核验**：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.2` 的 `LY-Space-Setup-0.5.2.exe`（92,537,816 字节）、对应 blockmap 与 `latest.yml` 均为 `uploaded`。下载到本地的安装包 SHA-256 为 `ABD4F765BA60D597B78EAB897F8AB317EC73D4DFCC66C08E06493DF65D766754`；`latest.yml` 的版本、文件名、大小及 SHA-512 均与该安装包一致。

### 3.29 v0.5.3 上游安全适配

- **缩放与编辑**：节点缩放期间临时收起编辑面板和右键工具栏，松开、窗口失焦或节点卸载后恢复；图片和文本已有内容节点的未提交提示词写入 `composerContent`，展开编辑器与行内输入实时同步。
- **文本数量兼容**：新增可选 `textCount`（1–15）。新建配置节点默认 1；历史配置节点没有该字段时继续回退原 `count`，普通文本节点未设置时仍生成 1 个，不改变图片批量生成。
- **来源与错误提示**：无响应 `ERR_NETWORK` 提示网络、DNS、TLS、Base URL 与 CORS 排查；Freestylefly GPT Image 2 提示词源默认关闭，用户手动启用后才下载缓存。
- **发布核验**：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.3` 为正式版；Actions `32222933180` 成功。远端 `LY-Space-Setup-0.5.3.exe` 为 92,538,498 字节，`latest.yml` 版本/文件名/大小与远端资产一致，SHA-512 为 `DNv7emKcvwqPQbQIvvzXx7RrGQj9Ejyl7JBbKTOXnu4AtbJnBJfVuMBbWPWDmi7FFa4kSGBpR8cxCZb9G9b91Q==`。本地验证安装器 `release\LY Space Setup 0.5.3.exe` 为 92,538,455 字节，SHA-256 为 `8DC659293CB1243C8A0A39B246945CD6C955C4357B2CF110797D0E4B4615E3EA`；本地与 CI 构建时间不同，字节大小不相同。

### 3.30 v0.5.4 生成中详情与中文输入法首轮处理

- **生图生成中详情**（`pages/image/generation-detail.ts` + `pages/image/index.tsx`）：按生成批次维护内存态槽位，单张图片成功后即可通过 `image.id` 找到所属批次并打开详情，不再等待整批结束和 IndexedDB 日志落库；详情实时统计成功、失败、取消与进行中数量。单图重试使用独立临时批次，日志或图片转存失败时只要已有成功图片，仍保留内存详情兜底。
- **画布中文输入法（首轮）**（`lib/keyboard-event.ts` + 画布编辑组件）：统一识别 `compositionstart/compositionend`、`isComposing` 与旧式 `keyCode/which=229`，并为内容编辑器和标题输入补充组合态快捷键隔离；文本节点、节点标题、节点提示词、展开编辑器、配置编排、项目卡片标题和画布标题均标记 `data-canvas-shortcuts-ignore`。该版本的文本节点正文受控 `<textarea>` 在组合态跳过 `onChange`，会被 React 回写旧值而导致首个拼音字母异常，后续修复见 3.31。
- **验证边界**：提交/tag 为 `2d1a82d8d0a65e9706fe995cbf7dd391e879631e` / `v0.5.4`；发布时本地打包通过类型检查、Vite 生产构建、NSIS x64 打包和包校验。仓库新增 `image-generation-detail.test.ts` 与 `keyboard-event.test.ts`，但该版本未执行自动化测试，真实中文输入法和生成中详情仍以 `pending-test.mdx` 的人工验收为准。
- **发布核验**：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.4` 为正式版；Actions `32228014575` 成功。远端 `LY-Space-Setup-0.5.4.exe`（92,538,923 字节）、blockmap（97,977 字节）和 `latest.yml`（344 字节）均为 `uploaded`；三份远端资产的 SHA-256 分别与本地 `release\LY Space Setup 0.5.4.exe`、对应 blockmap 和 `latest.yml` 一致。EXE SHA-256 为 `55269B2BA6FEA7B790F3D7DB28F37DBBEE7E11474D1B793D0F9CDEAE4E1EEB3B`，`latest.yml` 的 version/path/size 与 EXE 一致，SHA-512 为 `yHnETXVdKqN4iGZ/bUCCD8XjrVm1NdpJeYxec6MUycXw+abeVtf3z15ye/EOebaXXJYdiFrqBzhevARjbvGmXg==`。

### 3.31 Unreleased 受控文本框中文组合输入修复

- **根因与修复**（`canvas-resource-mention-textarea.tsx`）：文本节点正文是受控 `<textarea value=...>`；若组合态跳过 `onChange`，React 会把 DOM 恢复为旧值并打断拼音输入。现在组合态也同步文本值，`@` 引用匹配、光标测量和覆盖层刷新则延后到稳定输入或 `compositionend`，避免半成品拼音触发副作用。
- **全量快捷键核查**：节点提示词、展开编辑器和配置编排器继续使用 DOM 临时组合态；节点/画布/项目标题保持同步回写并忽略组合态 Enter。自定义尺寸与插件 URL 也忽略组合态 Enter，搜索框、新增文本资产和蒙版提示词维持原有同步受控更新。
- **验证边界**：`keyboard-event.test.ts` 新增组合态受控值同步回归；尚未取得微软拼音、搜狗和微信输入法的真实桌面端人工验收，验收矩阵以 `pending-test.mdx` 为准。第三方插件自行实现的编辑器不在本次改动范围。
- **当前工作区保护**：`canvas-config-composer.tsx` 还有未提交的配置编排器组合输入修复，避免 `contentEditable` 在输入法候选阶段被重建，并延后到最终 `input` 后同步内容。该修改尚未发布，后续任务必须原样保留并单独核验，不得用还原工作区的方式清除。

### 3.32 v0.5.6 精修工作台参考图重构 + 配置编排器组合输入修复

- **状态边界**：源代码随 `v0.5.6`（2026-08-24）发布；发布前自动化检查（typecheck / vitest 50 项含新增 9 项 / 渲染构建 / git diff --check）通过；**人工验收清单在 `pending-test.mdx` 顶部，验收通过前不得更新 features.mdx**。
- **页面结构**：`pages/refine/index.tsx` 改为全高三段式（顶部标题与图片入口 / 预览区+右侧约 400px 设置面板 / 底部撤销重做与导出发送操作栏）；lg 断点横向双栏，窄窗口纵向排列、外层滚动。预览组件 `pages/refine/components/preview-stage.tsx`、设置面板 `pages/refine/components/settings-panel.tsx`（页面私有组件目录）。
- **强调色遵循全局主色**：用户明确否决参考图的紫色强调——所有选中态/指示条/主按钮均用全局主色（antd colorPrimary 黑白中性 + stone 系），不得再引入独立强调色（已沉淀进 AGENTS.md）。
- **预览交互**：复用并扩展 `use-image-editor-viewport`（画布共享 hook，裁切弹窗同源）——新增可选 options（`minZoom`/`maxZoom`/`fitUpscale`/`maxAbsoluteScale`）与 `zoomTo`/`zoomToActualPixel`，默认值零行为变化，既有调用方不受影响。精修传 `fitUpscale`（小图铺满）+ `maxAbsoluteScale: 8`（按图片实际像素显示比例，上限 800%）；「原图」= 相同裁切与几何变换、不应用滤镜/调色/LUT 的渲染版本（激活时才渲染）；「对比」= 双层 img + `clipPath inset` + 可拖动/方向键调节的 role=slider 分割线（左原图/右效果）；Escape 关闭临时视图；缩放/平移/对比位置不写入会话。
- **编辑历史**：新建 `lib/refine-history.ts` 纯函数（`refineCommit`/`refineUndo`/`refineRedo`），undo/redo 返回扁平状态可直接链式传递；**会话兼容**：`RefineSession` 仅新增可选 `future` 字段，旧字段 `history/historyIndex/edits` 语义不变，旧草稿恢复后可撤销、不可重做。滑杆 `onChange` 预览（`previewEdits` 不进历史）+ `onChangeComplete` 提交（`commitEdits` 一条历史）。
- **其他要点**：`CanvasNodeCropDialog.onConfirm` 增加可选第二参数 `ratioPreset`（画布调用方零改动），精修确认后同步主页面比例状态；页面级 Ctrl+V 粘贴（排除输入控件）；`validateRefineSource`（`lib/refine-image.ts`）统一校验 JPG/PNG/WebP + 50MB（上传/拖入/剪贴板/资产/AI 结果同一入口）；AI 模式与补充要求内聚在设置面板，能力检查与失败反馈不变。
- **坑**：① `useImageEditorViewport` 的 zoom 是「相对适应基准」的倍率，绝对比例显示用其 `imageScale`，绝对上限经 `maxAbsoluteScale` 由 hook 内部换算；② 预览图 natural 尺寸变化（裁切/换图）会触发 hook 重置回适应，同尺寸调色重渲染不重置——不要在 img onLoad 之外另设重置时机；③ 对比模式两层 img 的 `onLoad` 只挂普通模式那张，imageSize 以普通模式加载为准。
- **配置编排器组合输入修复**（`canvas-config-composer.tsx`，用户随版合入）：contentEditable 编辑器在输入法组合阶段（`composingRef`）跳过 DOM 重建，避免候选词区被重建打断拼音并遗留首字母；`compositionend` 后下一帧再读 DOM（部分输入法最终 input 晚于 compositionend）；@ 引用匹配与覆盖层刷新延后到稳定输入帧执行；快捷键在组合态整段忽略。
- **发布核验**（2026-08-24）：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.6` 为正式版；Actions `32651593335` 成功。远端资产 `LY-Space-Setup-0.5.6.exe`（92,545,593 字节，GitHub API digest SHA-256 `2522537f628b95768457ec68586e0712ee0800456f07b37ac8326346b3acdce4`）、blockmap（97,887 字节）与 `latest.yml` 均为 `uploaded`；`latest.yml` 的 version `0.5.6`、path/文件名、size 92545593 与远端 EXE 一致，`files` 条目与顶层 sha512 相同（`E9El8v5WcLU4mdxOnWCIlJZ+8l7JQlIa2R7gOYMC2eo6QUM2kh5uxylVKou+T6TnZE2q+cRF6uCI9qwRx+ELeg==`）。本地安装包 `release\LY Space Setup 0.5.6.exe` 为 92,545,455 字节（88.26 MiB，verify-package 通过），SHA-256 `C8A321E014A68B67182B0250EFF65B168DCEE281FA8746BFEABC8538B7B67DA3`；本地与 CI 构建时间不同，字节大小与哈希不同属预期（同 3.29 先例）。
- **本地打包坑（pnpm v11.18）**：`pnpm --dir desktop run pack:win` 首次失败于 `ERR_PNPM_IGNORED_BUILDS: electron-winstaller@5.4.0`——`desktop/pnpm-workspace.yaml` 里被写入占位 `allowBuilds: electron-winstaller: set this to true or false`，必须明确决定。已设 `false`（本项目仅打 NSIS 包，不需要 Squirrel.Windows 的 electron-winstaller 构建产物），`pnpm install` 与打包随即通过；后续环境遇到同类占位按此处理，勿删文件。

### 3.33 v0.5.7 画布修复与可选 Agent 功能插件

- **画布修复**：批次根节点的加载/失败内容与「一键重试」双轴居中；所有可生成节点保留生成按钮并显示独立停止按钮，支持同源与跨节点并行；`@` 引用输入统一规避中文输入法组合态候选键。
- **功能插件**：桌面“设置 → 插件中心”只携带轻量宿主。`agent-core` 与 `skill-manager` 由用户显式安装，原节点插件系统保持独立；Agent 服务、Skill 管理和 Codex 运行时不进入桌面包。主进程验证官方清单、大小与 SHA-256，使用 staging 原子切换和可续传 `.part`；本地 Agent 只监听随机 `127.0.0.1` 端口，令牌不暴露到渲染层。
- **运行时与发布**：系统 Codex 仅在实际执行 `--version`、版本范围匹配且服务握手成功后复用；WindowsApps 路径存在但拒绝访问视为不可用。功能插件构建资产 Agent Core 约 1.55 MiB、Skill 管理约 5.6 KiB，Codex 运行时独立约 144.66 MiB；发布工作流 `publish-feature-plugins.yml` 成功发布资产，客户端无自动下载。
- **验证边界**：本地版本检查、76 项自动化测试、功能插件构建、`git diff --check` 和 `pack:win` 均通过；本地安装包 `release\\LY Space Setup 0.5.7.exe` 为 92,559,250 字节（88.27 MiB，SHA-256 `32F9FC5B29CF6524B02B053827C367A253B2AA11A8BE97584701170B7DBA987D`），包校验确认不含 Codex、Canvas Agent 或功能插件产物。真实 API/MCP/账号授权、网络受限下载与输入法人工验收仍以 `pending-test.mdx` 为准。
- **发布核验**（2026-08-25）：Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.7` 为正式版；Actions `32804217256` 成功。远端 `LY-Space-Setup-0.5.7.exe`（92,559,226 字节，GitHub digest SHA-256 `bed68114ba2dc8b7835dcd396db3dfeaf79ce2652dfe825c49d9a5065efefc7d`）、blockmap（97,980 字节，SHA-256 `6ccaa73ed5db1836057d6f14ad99e2c78e735aec08e76cea386674c7a5540420`）与 `latest.yml`（344 字节，SHA-256 `4516aa006a012ea24479879e9a611eac04ca305cec28cd0f79215725f9ff2cfa`）均为 `uploaded`；远端 `latest.yml` 的 version `0.5.7`、path/文件名、size `92559226` 与远端 EXE 一致，SHA-512 为 `0exgdCeRgtp/SDGFctKaxNbVlpIVEH8mxYmGLkOeDwSUiVxtsvg2pD6ZOWRjfdxuOs29TjmK3ck7GbBd/nNH/w==`。本地与 CI 构建时间不同，字节大小与哈希不同属预期。

### 3.34 v0.5.8 功能插件最低版本判断热修复

- **根因**：官方清单把 `minAppVersion` 写为裸版本 `0.5.6`，而 v0.5.7 的通用范围判断把裸版本按精确等值处理，导致 v0.5.7 被误判为“不支持 Agent Core”。
- **修复与兼容**：主进程新增最低版本专用判断，裸 `minAppVersion` 按 `>=` 解释；依赖和 Codex 运行时仍保留精确范围语义。功能插件构建器同步发布显式 `>=0.5.6` 清单，且已刷新 jsDelivr 缓存；v0.5.7 点击插件中心“刷新清单”后即可恢复安装，v0.5.8 还覆盖了未来裸最低版本清单。
- **验证与发布核验**：新增 4 项功能插件单测均通过；`v0.5.8` 本地打包通过（88.27 MiB）。Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.5.8` 为正式版；Actions `32804913317` 成功。远端 `LY-Space-Setup-0.5.8.exe`（92,559,495 字节，SHA-256 `b506c2a153a3d5bc7226270a5b565b3e3a4883846db3377685b6cb9a80a93b23`）、blockmap（97,933 字节，SHA-256 `7e24af44d22ddb6ecefa0ad31d789b5589fa5eae77ab27b5cf96deb9c30412b3`）与 `latest.yml`（344 字节，SHA-256 `76e906bf35f5453e2a65a903ecef57f40b9e3eb8033a2fb8f7d29784fadd5445`）均为 `uploaded`；远端 `latest.yml` 的 version `0.5.8`、path/文件名、size `92559495` 与远端 EXE 一致，SHA-512 为 `lKrv6rkuUXzqjU+aNc5WIpaVjJ5taOIj6qjI2ykpHQkdsgGv9FfPSMf0kfqIlNYy5ibzWKtmNLn9XAwocgmEGA==`。

### 3.35 v0.6.2 参考图 R2 自动上传与私有 Bucket 读取

- **范围与状态**：已随 `v0.6.2` 正式发布。渲染层类型检查、Vitest（69 项）和 `git diff --check` 已通过，Windows CI 安装包与 `latest.yml` 已远端核验；Worker 部署与真实 R2/Agnes 验收仍以 `pending-test.mdx` 为准。
- **工作台入口**（`components/reference-image-uploader.tsx`）：生图与视频页共用同一参考图组件。支持 PNG/JPG/JPEG/WEBP 的点击、多选、拖拽和 Ctrl+V；文件先显示本地预览，队列最多 3 项并发，成功后才将 Worker 返回的 HTTPS URL 加入当前任务参考图。失败项可重试或移除，已成功图可排序、复制链接、从最近上传复用或补充手动 HTTPS URL；移除仅清除当前任务引用，**不得删除 R2 对象**。
- **配置与错误边界**（`services/media-hosting.ts`、`services/oss-hosting.ts`）：自动上传只接受归一化后的 `cloudflare-r2` 配置，缺 Worker 地址、上传令牌或 HTTPS 公网地址时引导到「OSS设置」；仅允许 100MB 内图片。浏览器以 `FormData` 的 `file` 字段上传，禁止手工设置 multipart `Content-Type`。客户端兼容 Worker 返回 `publicUrl`、`url` 或仅 `key` 三种格式；401/403、413、415、5xx 会映射为可读错误，不展示令牌。
- **Worker 协议**（`cloudflare/r2-media-worker`）：`POST /upload` 同时兼容旧二进制请求和新的 multipart 请求；成功返回 `ok/success/key/url/publicUrl`。`GET`/`HEAD /<object-key>` 从 R2 读取对象并以 Worker 的 HTTPS 地址提供给模型，因此 Bucket **无需开启 R2 Public Access 或 r2.dev**；写入端点仍必须校验 `Authorization: Bearer UPLOAD_TOKEN`。`PUBLIC_BASE_URL` 必须是该 Worker 或自定义 Worker 域名，不是 Bucket 公网域名。
- **接手注意**：新增的 `desktop/renderer/src/components/reference-image-uploader.tsx` 与 `desktop/renderer/src/services/media-hosting.ts` 在提交时必须显式 `git add`。真实部署前先读 `cloudflare/r2-media-worker/README.md`，人工矩阵已补在 `pending-test.mdx` 首项；不能把现有 mock 测试改动当成真实 Worker/R2 或 Agnes 成功证据。

### 3.36 v0.6.2 画布 `@` 引用连续插入修复

- **根因与修复**（`components/canvas/contenteditable-reference-utils.ts`）：已有引用 chip 在序列化提示词时可能代表完整节点标记或标签，但 DOM 光标偏移只按一个节点位置计算；连续插入时删除 `@` 查询词的范围因此错位。光标前逻辑文本现在统一将每个 chip 记为一个占位字符，与 Range 偏移保持一致。
- **范围与兼容**：生成配置「组装提示词」、节点提示词和展开编辑器共用该工具，选择引用后会完整替换 `@` 或 `@查询词`，不留下 `@`。生成配置仍序列化为 `@[node:id]`，普通节点仍保留图片/文本标签；文本节点受控 textarea 的既有字符串替换和 IME 同步逻辑不变。
- **验证边界**：新增 jsdom 回归覆盖连续三次引用、查询词替换、普通文本保留和两种序列化格式；渲染层 Vitest 69 项和类型检查通过。真实 Windows 桌面端的鼠标/Enter 选择及微软拼音、搜狗、微信输入法仍按 `pending-test.mdx` 人工验收。

### 3.37 v0.6.7 手动画布恢复、非阻塞扫描与诊断详情

- **交互边界**（`pages/canvas/index.tsx`）：移除进入画布库后的自动扫描；“恢复画布”是唯一入口，点击立即打开对话框并显示真实的 `checked / total` 进度。扫描期间可关闭对话框，后台任务继续完成但不得重新打开；扫描只读备份，不写入当前画布。
- **性能与校验**（`canvas-recovery.cjs`、`main.cjs`）：安全快照、目录清单、复制和临时目录清理均使用异步文件 API。复制前后只比对 `path / length / mtimeMs` 轻量清单，确认副本完整且来源未变化；不再用备份 manifest 的全量 SHA-256 阻断可读来源。隐藏 BrowserWindow 仍负责必要的 IndexedDB/LevelDB 解析。
- **诊断与坑**：每个升级备份检查完成都会向渲染层发进度事件；不可读来源继续扫描其他来源，运行日志保留原始错误用于本机排查，导出 JSON 仅保留 `sourceType` 与将盘符/UNC 路径替换为 `<路径>` 的错误文本。自动化检查不等同于问题电脑的真实 LevelDB 可读性；8 个来源的最终结论必须查看该机日志和导出的 `failedSources`。
- **验证与发布核验**：`canvas-recovery` Node 测试 9/9、渲染层类型检查、主进程语法检查、版本校验和本地 `pack:win`/包校验均通过。提交/tag 为 `febe9e0` / `v0.6.7`；Release `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.6.7` 为正式版，Actions `33152913258` 成功。远端 `LY-Space-Setup-0.6.7.exe`（92,576,174 字节）、blockmap（98,046 字节）与 `latest.yml`（344 字节）均为 `uploaded`；`latest.yml` 的 version `0.6.7`、path/文件名、size 与远端 EXE 一致，SHA-512 为 `elXwR77CreRRqTqZQAGv5XZSSusCqe6oAxxmkuA+8xuu3KcFWLSdiHgDlzQAuRcZ4e0HEiICpKux6oUVOIR58A==`。本地留存安装包为 `release\\LY Space Setup 0.6.7.exe`，92,576,165 字节、SHA-256 `DA045056949A9A76B28AC33019114FCCA3F1081A84E0BF99E99D7292990F5FD1`；本地与 CI 构建时间不同，字节与哈希差异属预期。

### 3.38 v0.6.8 火山方舟 API 完整适配（已发布）

- **范围**：渲染层为 `ApiCallFormat: "ark"` 补齐现有文本、图片、视频工作流；只使用 Bearer API Key，不实现 AK/SK。标准方舟根地址为 `/api/v3`，Agent Plan 根地址为 `/api/plan/v3`；地址构造会处理尾部斜杠及误粘贴的完整接口路径，避免重复追加 `/responses`、`/images/generations` 或视频任务路径。
- **配置与界面**（`use-config-store.ts`、渠道编辑器）：旧配置自动补 `arkThinkingMode: auto` 和默认开启的 `imageWatermark`，不覆盖既有渠道、模型或密钥。标准方舟可尝试 `/models` 并按模型名推断图片/视频能力；Agent Plan 不伪造列表，模型选择器会说明需按账号套餐手动添加模型 ID。文本设置显示“自动/开启/关闭思考”；Seedream 隐藏不支持的质量/透明背景，改为水印；Seedance 显示比例、清晰度、时长、声音、水印和种子，Fast 的 1080p 保留原选择但禁止提交。
- **协议与错误边界**（`services/api/ark.ts`、`image.ts`、`video.ts`）：Responses 使用 `thinking.type`，保留 SSE、非流式 JSON 回退、系统提示词、图片输入及函数调用解析，不发送 OpenAI `reasoning.effort`。Seedream 统一使用 `/images/generations`：无参考图不发 `image`，单图发字符串、多图发数组；单张为 `sequential_image_generation: disabled`，多张为 `auto + max_images`，并只发送 `size`、`response_format`、`stream: false`、`watermark`。蒙版请求明确阻止。Seedance 发送三类参考资产、比例/分辨率/时长/声音/水印和合法 seed；服务层再次校验 9/3/3 数量、格式和总时长，停止生成后尽力 DELETE 远端任务，失败仅记录已脱敏诊断。
- **验证与发布核验**：新增方舟协议回归覆盖地址、三种思考模式、SSE 增量、函数工具字段、Seedream 文生图/单图/多图参数、Seedance seed/Fast/素材限制。Node 测试 34/34、渲染层 Vitest 79/79、TypeScript 检查和 `git diff --check` 已通过。发布提交/tag 为 `91063f6` / `v0.6.8`；桌面 CI `33868823083` 已成功。正式 Release 为 `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.6.8`，非 draft、非 prerelease；远端 `LY-Space-Setup-0.6.8.exe`（92,578,074 字节，SHA-256 `a025c92791a030840148606821380782ab25aab26335281ba1a71f1fa48f9d2d`）、blockmap（97,936 字节）和 `latest.yml`（344 字节）均为 uploaded。未使用真实 API Key，未进行可能收费调用。

### 3.39 v0.6.9 方舟桌面 CORS 全链路修复（已发布）

- **根因与范围**：v0.6.8 的方舟标准 `/models`、Responses、Seedream 与 Seedance 仍有渲染进程网络路径，浏览器会因上游没有 CORS 响应头而在请求到达方舟前拦截。v0.6.9 将这些方舟路径统一改为 Electron 主进程 HTTPS 代理；标准方舟与 Agent Plan 都覆盖，Agent Plan 仍不伪造模型列表。
- **IPC 与安全边界**（`main.cjs`、`preload.cjs`、`services/api/ark.ts`）：普通请求回传 HTTP 状态、正文和白名单 `content-type` / 方舟请求 ID；方舟专用限制为 64 MiB 请求体、32 MiB 响应体、120 秒超时、6 项并发。Responses 的 SSE 分片经 IPC 即时转发；AbortSignal 同时请求本地停止与主进程取消。运行日志只写方法、脱敏 URL、状态、耗时和请求 ID，绝不写 Bearer Key 或正文。
- **调用与验证**（`image.ts`、`video.ts`、`ark-adapter.test.ts`）：模型列表、Seedream、Seedance 创建/轮询/取消及 Responses 已改用共享适配层；非桌面开发环境保留浏览器直连，并在 CORS 网络失败时说明限制。已执行 Node 测试 34/34、渲染 Vitest 81/81、TypeScript 检查、主进程语法检查与 `git diff --check`；没有使用真实 API Key 或产生收费调用。发布提交/tag 为 `fda0feb` / `v0.6.9`，桌面 CI `33870358330` 成功。正式 Release 为 `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.6.9`，非 draft、非 prerelease；远端 `LY-Space-Setup-0.6.9.exe`（92,580,310 字节，SHA-256 `fb04dfb2c0df4d3177b7313817e313cabf70f55e6f462acf255bd90f5ec4d36e`）、blockmap（97,924 字节）和 `latest.yml`（344 字节）均为 uploaded。`latest.yml` 的 version `0.6.9`、path、size 与 EXE 一致，SHA-512 为 `jBrRCpOMX75ThycxZoi6mCwW6GB7t/lKjFxZ4WeQwQrdcFoOQunus6eXV9x/C5lGtS1Uey+rbbbc/18+f1wPUg==`。本地安装包为 `release\\LY-Space-Setup-0.6.9.exe`，其 SHA-256 与远端 asset digest 一致；`.mimosa/` 与 `desktop/.mimosa/` 保持未改动。真实账号权限、套餐模型、CORS、素材上传、结果质量和远端取消结果仍按 `pending-test.mdx` 人工验收。

### 3.40 v0.7.0 全渠道模型分类与识别（已发布）

- **结构化目录**（`lib/model-catalog.ts`、`services/api/image.ts`）：OpenAI 兼容、Gemini 与标准方舟的模型列表不再压成名称数组，而是保留上游已有的显示名、说明、归属、分类、能力、输入/输出模态和支持方法；GRS AI、Agnes AI 复用可靠内置清单。分类优先级为上游字段 → 内置清单 → 名称规则 → 未识别，不会为模型详情产生逐项额外请求。
- **能力与安全边界**：展示顺序固定为文本生成、视觉理解、图片生成、视频生成、音频生成、未识别、暂不支持；视觉理解在底层映射为文本能力。Embedding、Rerank、Moderation、Guard、ASR、声音复刻、3D 等未接入类型优先归入“暂不支持”且禁止新增选择；未知模型保存为 `unknown`，用户在渠道编辑器明确指定能力前不进入生成模型选择器。
- **弹窗与兼容**（`model-select-modal.tsx`、`channel-editor-drawer.tsx`）：保留新获取/已有页签、搜索、手动增加和勾选流程，新增带数量的能力筛选、按“能力 · 系列”折叠分组、分组选择、全部展开/收起及来源标签。搜索期间自动展开命中组，清空搜索恢复原展开状态；全选只影响当前可见可用范围。重复拉取只刷新上游展示信息，原能力、调用脚本和图片特性优先保留；旧配置会补推导展示字段，导入导出保留新增字段且继续拒绝导入脚本。
- **验证与发布核验**：新增 `model-catalog.test.ts` 13 项，覆盖 OpenAI/方舟基础对象、兼容平台非标准字段、Gemini 元数据、内置清单、上游优先、视觉映射、未知/禁选、筛选分组、搜索展开、手动增加、重复合并和旧配置。Node 测试 34/34、渲染层 Vitest 94/94、TypeScript、版本一致性和 `git diff --check` 均通过；未使用真实 API Key。发布提交/tag 为 `afe88ea` / `v0.7.0`，桌面 CI `33873193151` 成功。正式 Release 为 `https://github.com/LightyearXizIl/LY-Space/releases/tag/v0.7.0`，非 draft、非 prerelease；远端 `LY-Space-Setup-0.7.0.exe`（92,585,691 字节，SHA-256 `d51f675cda84927286f02e85fb4cab8b36ca0606a2cc9b22e09b516081a18efb`）、blockmap（98,022 字节，SHA-256 `5971421a4584ac8e2cc83b1e844f7a1c48b973a05300729263feb8d6ee99cd97`）和 `latest.yml`（344 字节，SHA-256 `7d1a9f27b356dcc9ae94ff08057773d34f2d8b4be444be868de56e55c2455a02`）均为 uploaded。`latest.yml` 的 version `0.7.0`、path、size 与远端 EXE 一致，SHA-512 为 `gDUKVbUqzJEfaLsCuy3hk4qVf0T/p8Owx+iIpTq583lHuONve0iFFaCtVTuIqbkXrI805/tKh/BWGvvA00+3GA==`。本地安装包为 `release\LY-Space-Setup-0.7.0.exe`，其 SHA-256 与远端 asset digest 一致；`.mimosa/` 与 `desktop/.mimosa/` 保持未改动。各平台实际返回字段、账号模型权限和桌面人工交互仍按 `pending-test.mdx` 核实。

### 3.23 v0.4.7 变更要点

- **升级数据保护重做**（`desktop/build/nsis-custom.nsh` + `backup-user-data.ps1` + `storage-migration.cjs`）：安装前等待旧进程退出，对安装目录 `Data cache` / `Result` 和旧 AppData 分别做递归 SHA-256 快照；复制或校验失败时安装器立即中止。新版首次启动使用临时目录校验并原子替换，只恢复当前安装数据，旧 AppData 单独留档且不混合 LevelDB。
- **默认数据移出安装目录**（`desktop/main.cjs`）：`sessionData` 固定到 `%APPDATA%\LY Space\Data cache`，生成结果固定到“文档\LY Space\Result”，自定义路径不变；移除旧的安装目录迁移与 `app.setPath("cache")` 覆盖，后续更新只替换程序文件。
- **GRS AI 参考图适配**（`services/api/image.ts`）：新版 `/v1/api/generate` 的 `images` 中，本地参考图发送裸 Base64、HTTP(S) URL 原样传递；请求体回归覆盖六个默认图片模型。
- **发版硬门槛**：升级前后逐文件哈希一致、应用重开后所有本地数据可读、GRS AI 两个模型家族最小真实调用成功，任一失败不发布。v0.4.7 Release 日志首条必须保留升级前人工备份提醒。

### 3.22 v0.4.6 变更要点

- **组节点框内锚定**（`project.tsx`）：① `handleNodeMouseDown` 组拖动时，`dragIds` 扩展为「groupId 成员 + 中心点落在组框架内的所有节点」（复用 `findContainingGroupId` 中心点判定）——**放进组内的任意节点自动跟随组移动**，松手后经 `finishNodeDrag` 的 `findContainingGroupId` 自动写入 groupId 归属为组成员；② `isNodeLocked` 同步改为「groupId 成员 + 中心点在任一固定组框架内」都锁定（嵌套组场景直接按几何判定，不再依赖 groupId 链）。**坑**：拖动组时若某节点同时落在多个组框架内，`findContainingGroupId` 按数组逆序取最后匹配的组；组自身不参与自身框内判定（`child.id === node.id` 跳过）。
- **组节点右上角布局**（`canvas-node.tsx` GroupNodeContent）：头部改 `justify-between` 两段式——左侧 [组图标 + 「组」]，右侧 [钉子按钮 + 「N 个节点」标签]，明确置于右上角（原 `ml-auto` 单点推送在部分情况下视觉上靠左）。
- **图片/视频节点编辑面板还原重排**（`canvas-node-prompt-panel.tsx` + `canvas-node.tsx`）：v0.4.5 拆两行（提示词工具行左对齐在上）用户不满，v0.4.6 还原并重排——优化提示模块（文本模型+优化按钮）**移入底部工具栏、位于生成按钮左侧**，图片/视频节点行为一致；面板容器 `w-[600px]` 改为 `min-w-[600px] max-w-[min(92vw,1100px)]` 自适应拉长容纳全部功能模块。**坑**：① 面板宽度变化影响所有节点面板（文本/音频/配置共用容器），控件均已 truncate/min-w-0 安全；② 生成按钮 `shrink-0` 保持在最右；③ 文本/音频节点面板无优化模块，保持原样。

## 4. 常用命令

| 命令 | 用途 |
|---|---|
| `pnpm --dir desktop run check` | 类型检查（tsc --noEmit） |
| `pnpm --dir desktop run pack:win` | 本地打包到 `release/` |
| `node desktop/scripts/verify-release-version.cjs vX.Y.Z` | 发版前版本校验 |
| `node --check desktop/main.cjs` | 主进程语法检查 |

## 5. 发版本流程（用户要求"更新版本/发版本"时）

1. **更新交接文档**（本节）：版本号改 vX.Y.Z；新增小节记录 vX.Y.Z 变更要点与坑；用户偏好按需补充
2. `CHANGELOG.md`：Unreleased 整理成 `## vX.Y.Z - 日期`，保留空 Unreleased
3. `VERSION` → vX.Y.Z；`desktop/package.json`、`desktop/renderer/package.json` version 同步
4. `node desktop/scripts/verify-release-version.cjs vX.Y.Z` 校验
5. `git add -u`（**新文件要显式 add**）+ commit `release: vX.Y.Z` + `git tag vX.Y.Z`
6. `git push origin main` + `git push origin vX.Y.Z` —— tag 触发 CI（`publish-desktop.yml`），自动构建上传并**写入 Release 日志**
7. 用 `git credential fill` 取 token 验证 CI `completed/success` + release 资产（exe/blockmap/latest.yml 齐全 + body 非空）
8. 本地 `pnpm --dir desktop run pack:win` 留存安装包到 `release/`（用户习惯）
9. 注意：改版本号重发需删旧 tag/release（force push main + tag），参考 v0.2.0/v0.3.0 处理

## 6. 待办与待测

- `docs/content/docs/progress/todo.mdx`：画布插件稳定性、图像/视频/音频渠道兼容性
- `docs/content/docs/progress/pending-test.mdx`：Agnes 中国站（`api.agnes-ai.cn`）视频生成实测（平台维护中）、参考图托管链路、OSS 配置
- **精修工作台待验收**：3.32 参考图重构已随 v0.5.6 发布，人工验收清单见 `pending-test.mdx` 顶部（空状态/上传/缩放平移/原图对比/四页签/撤销重做/导出发送/多分辨率与主题）；验收通过前不得更新 features.mdx。
- 用户当前重点：画布中文输入法未提交修复的保护与人工验收、精修工作台参考图重构、画布交互体验（框选/连线/指针）、窗口与主题适配

## 7. 用户偏好

- 中文交流；代码注释/文案用中文
- **deepseek-eyes 视觉技能已配置**（见 3.17）：用户发图时自动走 `eyes.py`，默认 zhipu `glm-4.6v-flash`（免费）；精细看图切 gemini、文字提取切 siliconflow
- 直接修复确认的 bug（不要只报告）；低风险问题修完一起说
- 每次功能修复完成后用户通常要求发版本 + 推送源码和安装包 + 本地留存
- **每次发布新版本都要更新交接文档**：版本号、vX.Y.Z 变更要点与坑（见第 5 节发版流程第 1 步）
- **更新升级必须保留用户数据**：画布、生图/视频工作台数据、渠道配置等一律不得因升级删除（NSIS 宏 + userData 保障见 3.14）
- 遵循 `AGENTS.md`（最小改动、不重构、文档规范、不执行编译除非要求）
- 重要外部信息：Agnes 平台中国站 `platform.agnes-ai.cn`（密钥页 `/settings/apiKeys`）、官方文档 `www.agnes-ai.cn/zh-Hans/docs/agnes-video-v20`（Mintlify，支持 `.md` 后缀抓取）
